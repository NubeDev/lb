# Debugging — working history

The project's debugging memory: every issue and how it became working, so nothing is
debugged twice. **Append-only and symptom-led.**

- How this works and the entry template: `../scope/debugging/debugging-scope.md`.
- One file per issue, named by the symptom: `<area>/<symptom-slug>.md`.
- Add a row below when you open an entry; update its status when it closes.

## History (newest first)

| Date | Area | Symptom | Status | Entry |
|---|---|---|---|---|
| 2026-07-12 | datasources | Pre-existing (found during the pack-toolchain full-suite run): `lb-host federation_test::federation_end_to_end_postgres` aborts with a **stack overflow** (deterministic, SIGABRT after the in-test docker postgres + sidecar build). Not caused by the packaging change (diff touches only devkit internals/Cargo metadata; no host/federation source). May have been masked on docker-less boxes (the harness skips without docker). Owner: next federation/datasources session — hunt the recursion (sqlparser/polars plan?) on clean master | open | [extensions/federation-postgres-e2e-stack-overflow.md](extensions/federation-postgres-e2e-stack-overflow.md) |
| 2026-07-12 | flows | A matched `switch` plus plain wires into one node HUNG the run (never terminal) once the `flow-plain-wiring` flip made every port `any`-by-default: the matched release went through the barrier path unconditionally (`switch.rs` → `touch_barrier_slot`), seeding a Pending indegree-3 slot the sibling per-message firings never decrement. Latent before the flip for any multi-wire `any` sink downstream of a switch; named by peer review, fail-before verified (the regression test hangs on the reverted path). Fix = ONE policy-aware release seam: `run_store::release_one_dependent` (an `any` dependent gets a normal minted firing, `triggered_by = switch`; explicit-`all` keeps the barrier); the barrier-only `ready_one_dependent` shortcut is deleted so the class cannot recur. Regression: `matched_switch_into_a_multi_wire_any_port_reaches_terminal` + the gated and explicit-all-barrier siblings. Lesson: two release paths with different policy awareness is one latent hang — make the policy check structurally unavoidable (one seam), don't duplicate it | **fixed** | [flows/matched-switch-hangs-run-after-any-default-flip.md](flows/matched-switch-hangs-run-after-any-default-flip.md) |
| 2026-07-11 | extensions | `lb-ext-native`'s `serve::tests::health_replies_ok` (and other non-`shutdown` cases) **hang forever** run in isolation — masked in a full run by a later shutdown test exiting the process. Root cause: the `round_trip` harness dropped only the WRITE half of the host `duplex`, so the child never saw EOF and `server.await` blocked (a `duplex` closes only when BOTH split halves are dropped). Predates this session (repro'd on clean `sdk-v0.2.1`); exposed while adding the `lb-sidecar-client` dep changed process teardown timing. Fix: drop both host halves. Regression: the single-test run now returns green (the hang was the failure). Lesson: a split stream signals EOF only when every writing-side handle is dropped; a test that passes only inside a larger run is hiding a hang | **fixed** | [extensions/native-serve-test-eof-hang.md](extensions/native-serve-test-eof-hang.md) |
| 2026-07-11 | prefs | New `invite.email.*` catalog messages rendered as their KEY LITERALS — MF1 treats `'` as the escape char, so `You're` / `'{workspace}'` fail the subset parser and the never-blank chain degrades to the key. Fix: reword without apostrophes/quoted braces + regen the client twin; tests assert the rendered text, not just delivery | fixed | [prefs/mf1-apostrophe-quotes-break-catalog-message.md](prefs/mf1-apostrophe-quotes-break-catalog-message.md) |
| 2026-07-11 | frontend | minimal-shell's `@nube/ext-ui-sdk` was `link:../../lb-ext-ui-sdk` (inside the lb repo — nonexistent); invisible while only TYPES were imported, broke on the first runtime import (i18n seam). Fix: `link:../../../lb-ext-ui-sdk`; the i18n tests now exercise the runtime import | fixed | [frontend/minimal-shell-sdk-link-path-wrong.md](frontend/minimal-shell-sdk-link-path-wrong.md) |
| 2026-07-11 | cli | Pre-existing: `lb-cli reminder_test` round-trip is `Denied { tool: "reminder.create" }` over the real gateway (CLI test seed's cap fold, not a reminders regression) — the ONE allowed failure on the node-v0.2.0 tag; owner: next reminders/cli session | open | [cli/reminder-create-denied-in-cli-round-trip-test.md](cli/reminder-create-denied-in-cli-round-trip-test.md) |
| 2026-07-11 | frontend | minimal-shell: every SSE subscription silently 401'd — `POST /events/{sid}/subscribe` is header-authed but `events.ts` sent no `Authorization` header, and the fire-and-forget `fetch` swallowed the deny (stream open, zero subjects). Same pass: 401 handling cleared `lb.session` without notifying the session store (stale UI until reload; fixed via `lb.session.cleared` event) and `getSession` returned a fresh `JSON.parse` object per call — `useSyncExternalStore` snapshot instability = React render loop once logged in (fixed: cache by raw string). Regression: `App.test.tsx` (4 tests). Lesson: the events API is split-auth (query-token stream, header-authed control verbs) — a new client must copy both halves; surface non-OK on fire-and-forget control calls | **fixed** | [frontend/minimal-shell-sse-subscribe-401.md](frontend/minimal-shell-sse-subscribe-401.md) |
| 2026-07-11 | auth-caps | Invite accept wrote the credential BEFORE claiming redemption, and mark-accepted was a plain read-modify-write — two concurrent accepts both passed and the loser overwrote the winner's password. Fix: store-level CAS claim (`lb_store::create` of an `invite_claim` row — first write binds, racers get `Conflict`) taken before ANY mutation; post-claim failure releases the claim back to `pending`. Regression: `invites_hardening_test.rs::double_redeem_loses_before_credential_mutation`. | fixed | [auth-caps/invite-accept-credential-race.md](auth-caps/invite-accept-credential-race.md) |
| 2026-07-11 | inbox-outbox | Peer review: the push target's `effect_workspace()` **hardcoded `"acme"`** — every push effect, whatever ws it was enqueued in, resolved devices/prefs/disable-writes in `acme` (rule-6 cross-workspace hole; any other ws's push silently dead). Shipped green because `deliver()` had ZERO tests. Fix: the email-target pattern — `notify.send` embeds `"workspace": ws` in the effect payload at enqueue (already authorized there); `deliver()` reads it and FAILS the effect if absent (never guess a ws); audience now also membership-checked (`membership_is_member`) so a non-member `sub` is excluded. Same review pass: per-device delivered markers keyed by the effect's idempotency key (an outbox retry re-sends only the failures, never re-buzzes a delivered device; `collapse_key` rides to the provider for provider-side collapse) and ULID effect ids (`notify:{now}:{first_recipient}` collided within one second → the second notification was silently deduped away by the outbox). Regression: `host/tests/push_deliver_test.rs` — 7 tests through the REAL relay (`relay_outbox`): fan-out, token-gone auto-disable, quiet hours, non-member exclusion, partial-failure retry dedup, same-second ids, missing-ws fails | **fixed** | [inbox-outbox/push-target-hardcoded-workspace.md](inbox-outbox/push-target-hardcoded-workspace.md) |
| 2026-07-11 | inbox-outbox/prefs | Quiet hours never held: `set_user_prefs(push_muted: Some(true))` returned Ok but read back `None` — the muted member's device was sent anyway. The axis was added to `lb_prefs::Prefs` but NOT to the store's two mirrors in `prefs/store/schema.rs`: the SCHEMAFULL `DEFINE FIELD` list (SurrealDB silently drops an undefined field on write) and the `PREFS_COLUMNS` read projection. Fix: additive `DEFINE FIELD IF NOT EXISTS push_muted TYPE option<bool>` on both prefs tables + `PREFS_COLUMNS` entry (`IF NOT EXISTS` heals existing stores). Lesson: a new Prefs axis is THREE mirrors (struct + `DEFINE FIELD` ×2 tables + `PREFS_COLUMNS`); only a write-then-read-back-through-the-real-store test proves the axis exists | **fixed** | [inbox-outbox/push-muted-pref-silently-dropped.md](inbox-outbox/push-muted-pref-silently-dropped.md) |
| 2026-07-11 | files/media | Peer review: `PUT /media/{id}/chunk/{n}` was **authenticated but uncapped and unvalidated** — it called `chunk_write` straight after `authenticate()`, so any workspace member could write chunks without `mcp:media.upload:call`, to non-existent uploads, past the declared chunk count, oversize, and — worst — **re-PUT a chunk after commit**: the record stays `Ready`, served bytes change, the ETag stays stale (cache-poisoning-grade tampering with a 200). Plus `base64_decode` `unwrap_or_default()` served a corrupt chunk row as truncated bytes with a 200. Fix: new host verb `media/chunk.rs::media_chunk_put` (cap → exists → `Uploading` → `n<chunks` → `body≤chunk_size`, all before any byte lands; route maps 403/404/400/413) and `base64_decode` → `Result` (corrupt row = 5xx). Regression: 7 tests in `host/tests/media_test.rs` (cap-deny + ws-isolation on the byte path, post-Ready tamper, range/oversize, corrupt chunk). Lesson: a raw-bytes HTTP route beside an MCP surface is its own attack surface — the gate must live in a host verb the route calls; "authenticated" is not "authorized" | **fixed** | [files/unchecked-chunk-put-writes.md](files/unchecked-chunk-put-writes.md) |
| 2026-07-11 | auth-caps | Entity-scoped grants silently **widened to `Scope::All`** on two paths (peer-review findings): (1) a present-but-malformed `scope` selector on `grants.assign`/`revoke` hit `serde_json::from_value(...).unwrap_or(Scope::All)` in `host/authz/tool.rs::scope_arg` — the admin asked for `child:[leo]`, the store silently got an UNSCOPED grant (fail-open at the chokepoint); (2) `Scope::union` collapsed two `Ids` selectors for *different* tables to `All` (`if t1 != t2 → All`, mislabelled "conservative"), so `child:[leo]` + `site:[north]` under one cap resolved to every row of every table — and the test `union_ids_different_table_widens_to_all` enshrined it. Fix: `scope_arg` returns `Result` (malformed → `BadInput`, nothing written; absent/null still `All`); additive `Scope::Tables` variant accumulates per-table id-sets so the union never widens (single-table unions still collapse to `Ids` — `grant_id` keys byte-stable, zero migration holds); gateway `/admin/grants` also gained the optional `scope` passthrough it was hardcoding to `All`. Regression: `malformed_scope_selector_is_bad_input_and_writes_no_grant`, `multi_table_scoped_grants_reach_only_their_rows_not_everything`, `union_ids_different_table_accumulates_without_widening`, `union_across_tables_reaches_only_granted_rows_not_everything`, `malformed_scope_in_body_is_rejected_not_widened`. Lesson: at a narrowing seam every unexpected input must resolve toward LESS authority — an `All` nobody granted is an escalation, not a fallback | **fixed** | [auth-caps/entity-scope-widens-to-all-silently.md](auth-caps/entity-scope-widens-to-all-silently.md) |
| 2026-07-10 | auth | Reports shipped `mcp:report.save:call`/`report.export:call`/`brand.save:call` correctly in `AUTHOR_CAPS`, host+gateway tests green, but on the live dev store an admin opened **Reports → New report** and save/export were **denied** — the login token carried only the VIEWER-tier report caps (the AUTHOR-tier caps were missing). Root cause: a stale persisted built-in role record + a freshness asymmetry in `resolve_caps`. `ensure_builtin_authz_roles`→`ensure_one` is idempotent on absence (writes a `role` row only when absent), so a workspace seeded BEFORE the `report.*` caps keeps the frozen old `member`/`workspace-admin` rows forever; `resolve_caps` reads that stored record to expand a `role:<name>` grant, so a new built-in cap never reaches an already-seeded workspace's tokens. The viewer tier dodged it only because the login floor (`credentials.rs`) calls the LIVE `viewer_role_caps()`; author/admin caps ride the stored record with no live path. The throwaway `reseed_roles.rs` (delete + re-seed while the node holds the store lock) was a symptom fix. Durable fix: the resolver UNIONS the live built-in bundle on top of the stored record for a granted built-in role — a new built-in cap takes effect the moment code ships, no re-seed, no version bump. Because the resolver is in the pure `lb-authz` crate (no `lb-host` dep), the live bundles are injected via a `BuiltinRoleCaps` callback: `resolve_caps_with`/`_subject_caps_with`/`_sourced_with` take it; `host/src/authz/builtin_caps.rs::LiveBuiltinRoleCaps` maps the three names to `*_role_caps()`; `resolve_caps_live`/`resolve_subject_caps_live` are the canonical host entry points every caller (login mint, apikey, reminder, dashboard access_check, access console) uses. UNION not replace (an ext's `grant_assign(Subject::Role(name))` is still honoured); custom roles untouched. Rejected alt: version the role records (needs a const per cap change, stays stale between bumps, repairs-not-guarantees). Scope: `scope/auth-caps/builtin-role-freshness-scope.md`; `reseed_roles.rs`+`examples/` deleted. Regression: `authz/tests/builtin_role_freshness_test.rs` (4 — stale row → missing without the union, present with it; NoBuiltin equals raw; custom role unaffected; union keeps direct role-subject grants) + the existing `sourced_cap_set_equals_resolve_caps_no_drift` cross-check still passes. Lesson: two halves of one role-resolution path went stale at different rates (viewer live, author/admin frozen); a cap bundle defined in code but read from a seeded-then-frozen record is a time bomb — make the resolver AUTHORITATIVE for built-in names (read the live bundle), don't repair the stored row | **fixed** | [auth/builtin-role-row-frozen-stale-on-new-caps.md](auth/builtin-role-row-frozen-stale-on-new-caps.md) |
| 2026-07-09 | frontend | A `query`/`source` dashboard variable backed by `series.find`/`series.list`/`datasource.list` always resolved to an **empty option list** (bar showed only `(none)`) even with real ws-scoped rows + the tool's cap. Root cause: `resolveOptions.ts` `extractRows` understood only a bare array or a `{rows}` object (`store.query`), but the ingest read tools wrap their list under a tool-specific key (`{series}`/`{samples}`/`{datasources}`) — so it fell through to `[]`. The static custom/interval path masked it; no test had driven a `series.find`-backed variable end to end. Fix: try the known wrapper keys then fall back to the first array-valued property (id stays opaque, no per-tool branch). Regression: `resolveOptions.test.ts` "extracts the `series` array" + live in `variablesAdvanced.gateway.test.tsx` | **fixed** | [frontend/query-variable-options-empty-for-series-find.md](frontend/query-variable-options-empty-for-series-find.md) |
| 2026-07-09 | weather | `weather.current` on the live node returned `weather fetch failed: error sending request for url (…open-meteo…)` while all tests + a standalone `reqwest` probe got `200 OK`. Ruled out sandbox (node's parent is `make dev` ← VSCode, not a Claude bg task; same netns as shell), env/DNS/roots (a fresh process with the node's exact `/proc/PID/environ` got 200 OK), and stale binary (exe current, not `(deleted)`). Re-running the identical live call **now succeeds** with real data. Root cause: a **transient outbound-connectivity blip** at the earlier test's moment — no code change. Note for future: reqwest here resolves to `rustls-tls-native-roots` (via `polars-io`/`object_store`), NOT webpki-roots, so `SSL_CERT_FILE`/`SSL_CERT_DIR` *are* consulted at runtime — check the trust store first if this recurs on a stripped image; and reproduce with a probe that walks `Error::source()` since Display truncates DNS/connect/TLS into one string | **resolved** | [weather/weather-fetch-error-sending-request.md](weather/weather-fetch-error-sending-request.md) |
| 2026-07-09 | flows | A multi-input flow node fired **once** (an AND barrier over all upstreams), never **once per upstream** (Node-RED's fire-per-message OR) — the OR case was structurally inexpressible, and which semantics you got was a side effect of `indegree` + a lint, not an author decision. Two coupled defects (spine Decision 14's deferred port-labelled-edge model, executed here): (1) edges were bare node-id `needs` with no target-port label, so the engine couldn't tell "two wires into one input" (OR) from "two inputs to join" (AND); (2) a node fired exactly once per run under `{run}:{node}` — no seam for "fire once for A's message, then again for B's". Fix = the **firing context (`fctx`)** — a per-message identity carried in an additive envelope field: an `any` port releases once per settled upstream, minting a deterministic id (nested funnels extend it: `link-in#mqtt-a` → `…·funnel2#w`); every claim key + `${steps.*}` resolution is scoped by `(node, fctx)` — empty in the all-`all` case ⇒ **byte-for-byte today's key**; an `any`-firing auto-wires its single triggering upstream. Engine rewrite: frontier-only seeding + dynamic per-policy release (`all`=barrier decrement, `any`=mint firing); run-terminal counts slots not nodes (a gated-skip on a never-seeded dependent now CREATES a `Skipped` slot — the bug that hung `switch_fires_only_the_matched_port` post-rewrite); `sink`-kind ports (incl. `debug`) default to `any`, the join lint is per-port policy-aware. Regression: `any_funnel_fires_once_per_upstream` (3 wires ⇒ 3 settles, distinct payloads + `fctx` labels) + the byte-identical `all_join_barrier_settles_once_at_empty_fctx` + `workspace_isolation_any_funnel_step_keys`. Lesson: a semantics inferred from `indegree`+lint is a guess dressed as behaviour; multiplicity must propagate via a carried identity, not a depth-1 suffix (works at the funnel, collapses one hop down). **Propagate-one-hop-past-the-funnel end-to-end test deferred to Slice 3** (needs the non-sink `any` = `link-in`; the `fctx` machinery is built + unit-tested) | **fixed** | [flows/multi-input-node-fires-once-not-per-message.md](flows/multi-input-node-fires-once-not-per-message.md) |
| 2026-07-09 | deploy | Building the deploy image, `docker run` of `lazybones:local-test` started Caddy fine but the `node` process printed **zero** boot output and exited 1 with a bare `Error: No such file or directory (os error 2)`. Root cause: `main.rs` reads the `hello` extension manifest via a compile-time-baked path `env!("CARGO_MANIFEST_DIR")/../extensions/hello/extension.toml` (`/src/rust/node/../extensions/hello/...`); the runtime stage copied the target files but never created the now-empty `/src/rust/node` directory the `..` hop traverses through, so `openat` ENOENT'd on the missing intermediate component even though the resolved target directory existed. Found via `strace -f -e trace=openat` inside the built image (the bare error message named no path). Fix: `RUN mkdir -p /src/rust/node` in `deploy/common/Dockerfile`'s runtime stage before the manifest `COPY`. Verified: full local build → `docker run` boots clean, `deploy/fly/smoke.sh` all-green, a token minted before a container restart still verifies after (persistent `LB_SIGNING_KEY` works). Lesson: `CARGO_MANIFEST_DIR`-derived paths bake in the BUILDER's full absolute directory chain — reusing them across Dockerfile stages needs every `..`-traversed intermediate directory to physically exist, not just the leaf the file lives in | **fixed** | [deploy/node-boots-then-exits-hello-manifest-enoent.md](deploy/node-boots-then-exits-hello-manifest-enoent.md) |
| 2026-07-09 | deploy | Implementing the planned `deploy/common/.dockerignore` (one whitelist ignore file "shared verbatim" across local Docker/CI/Fly), `docker build -f deploy/common/Dockerfile .` failed `COPY docker/postgres/seed.py` with "excluded by .dockerignore" even though that exact file was whitelisted in `deploy/common/.dockerignore`. Root cause: Docker always resolves `.dockerignore` from the build CONTEXT ROOT, never from next to the Dockerfile — a per-Dockerfile ignore file needs `buildx build --ignorefile`, a buildx-only flag that plain `docker build`/`docker compose build` (local dev + classic CI) never pass. The repo's existing root `.dockerignore` (scoped to the desktop build) was the file actually governing the build, and it excluded `docker/postgres/` wholesale. Fix: deleted `deploy/common/.dockerignore`; the repo-root `.dockerignore` is now the one file every repo-root-context build shares, with a narrow `!docker/postgres/seed*.py` etc. re-include carve-out for the deploy image's demo-dataset generator. Lesson: "one ignore file, referenced by every driver" has to mean the same *file* — a same-content copy is silently dead weight for any driver that doesn't pass `--ignorefile` | **fixed** | [deploy/dockerignore-in-deploy-common-silently-ignored.md](deploy/dockerignore-in-deploy-common-silently-ignored.md) |
| 2026-07-09 | desktop | The `full` standalone desktop binary (`make windows-full`/`make linux-full`) booted, printed its loopback-gateway login line, and login **succeeded** — but the seeded `user:ada` was missing access to almost everything (every admin surface + most authoring surfaces 403). Cause: `ui/src-tauri/src/full.rs::seed_dev_identity` assigned the built-in role GRANTS (`role:member`, `role:workspace-admin`) but never seeded the built-in role RECORDS, so `resolve_caps` (which folds a subject's role-record cap bundles into the login token) found no `workspace-admin` record and the grant resolved to an **empty cap set** — the token minted with only the `dev_claims` base + viewer floor. The dev node (`rust/node/src/main.rs:31`) does this right by calling `lb_host::ensure_builtin_authz_roles(store, ws)` BEFORE the grants; `full.rs`, a near-verbatim copy, had dropped that one line. Masked by the existing `full_loopback_test`, which only asserted `tools.catalog` (a viewer-level read that passes regardless of the role fold), so the gap shipped green. Fix: add the missing `ensure_builtin_authz_roles` call (idempotent — writes a role record only when absent). On a single-workspace standalone node `workspace-admin` IS the "everything" role (every admin cap); `super-admin` is the reserved cross-workspace node-operator tier, not seeded per-workspace, so it adds no reachable surface here. Verified: rebuilt linux-full, decoded the login token → **220 caps incl. all 10 admin caps**, drove the real admin route `GET /admin/roles` → 200 listing `[member,viewer,workspace-admin]` (was 403, no records); then rebuilt windows-full. Regression: `full_loopback_test::seeded_user_resolves_to_a_real_workspace_admin` (drives the admin-gated `/admin/roles`; 403 without the fix, 200 with). Lesson: a role GRANT is inert without the role RECORD it resolves against — seed the bundle before the assignment; and a boot-seeder copied between two binaries must copy every line, not just the obvious ones | **fixed** | [desktop/full-seed-user-missing-admin-caps.md](desktop/full-seed-user-missing-admin-caps.md) |
| 2026-07-09 | rules | The catalog advertised `records(grid) -> Array<Map>` and the rules skill doc documented `category(query(...).records(), "name", "value")` as a chart-ready rule's last line — but on the **federation** source path (every sqlite/postgres source, including `demo-buildings`) `records()` returned positional **arrays**, not maps, so the one-liner failed with `category: every row must be a record`. Two seams feed the cage `Grid` with two different row shapes: platform (`store.query`/Surreal) returns JSON objects; federation (`extensions/federation/src/query.rs::shape`) deliberately re-projects Arrow objects to column-aligned arrays. `grid.rs::records()` forwarded whatever the seam returned — maps on platform, positionals on federation — and the cage unit tests (`chart_test`, `grid_test`) ran against `RecordingData::platform(...)` (object fixtures) so they masked the drift. The host render path (`viz::frame::result_to_rows`) was already correct via a separate columnar-zip arm; only the cage boundary was wrong. The committed buildings regression test asserted `rows[0][0]` and the three example bodies used `r[0]`/`r[1]` — both silently relied on the broken shape; the prior session doc even recorded "`.records()` returns positional arrays" as a "hard-won fact re-verified by the green test" (a fact about a bug codified as truth). Fix: collapse the two shapes at the seam boundary — `grid.rs` gained `row_to_map(row, columns)` (object→pass through; array→zip with columns into a map; scalar→single-cell map) and `records()` routes every row through it; the three buildings examples + their test assertions moved to named map access (`r.building`/`r.kwh_per_m2`); a new `buildings-intensity-chart` example was added whose last line is `category(rows, "building", "kwh_per_m2")` (the chart-ready rule the docs promised, now real on the demo dataset); `RecordingData::federation(...)` test helper seeds federation's real positional wire shape so the contract is unit-testable without the sidecar. Regression: `grid_test::{records_returns_named_maps_from_federation_positional_rows, category_runs_on_federation_records}` + the e2e `rules_buildings_examples_test` extended (chart body runs on real federation + `buildings.db`: 8 rows trimmed to label+value, Riverside 4.68 kWh/m²). Lesson: a catalog signature is a contract not a description; two shapes at a seam need exactly one normalizer at the boundary, and a "hard-won fact" recorded against a green test can be a fact about a bug — when the test asserts the broken shape it's the drift's hiding place | **fixed** | [rules/records-returns-positionals-on-federation.md](rules/records-returns-positionals-on-federation.md) |
| 2026-07-09 | rules | Building the rule `insight.raise`/`ack`/`close` handle (rule-raises-insight-scope), the `route:false` "a panel repaint raises nothing" decision assumed the run's `route` flag was reachable at handle-construction — it was NOT. Grepping first (the scope's risk #1) showed `route` lived ONLY in the host's post-run routing (`run.rs` `if route { route_alerts(...) }`, after `engine.run`); the `RuleEngine`/`register`/`RunHandles` never received it, because `alert()` suppression is host-side (findings collected, then not routed). An insight raise differs — it's a **cage-initiated** write by the rule body, and it's a *stronger* effect than `alert()` (durable record + notify fan-out; dedup collapses the record but not the count-bump/occurrence/notify re-fire), so the no-op MUST happen inside the cage. A naive handle (seam+meter+`now`, like `ChannelHandle`) had no `route` to check → would have raised + fanned out on every 30 s dashboard repaint. Fix: thread `route` into the cage, symmetric with `now` — `RuleEngine.route` (default `true` via `new`, set by `with_route(route)`; existing callers unchanged), `register(...)` gained `route`+`origin_ref`, `RunHandles` gained `insight: InsightHandle` built with the flag; the handle short-circuits BEFORE charging the meter on `route:false` (charges nothing, logs an honest skip line, returns an echoed id). `HostMessagingSeam` unchanged (generic `call_tool`, gates `insight.*` by `mcp:<tool>:call`, rule 10). Regression: `insight_test::{route_false_no_ops_every_method_and_logs_the_skip, route_true_writes_where_route_false_did_not}` + host `rules_test::route_false_run_raises_no_insight` (real `mem://`, count stays 0 vs 1). Lesson: a flag that "already exists" for one effect may not reach the layer a new effect needs it in — verify where a load-bearing flag actually lives before building behavior on it; a handle that silently has no flag to honor looks done and ships broken | **fixed** | [rules/insight-handle-route-flag-not-in-cage.md](rules/insight-handle-route-flag-not-in-cage.md) |
| 2026-07-09 | frontend/host | A panel/widget bound to a Rules source (`{tool:"rules.run", args:{rule_id}}`) rendered **zero rows** for every view through `viz.query`→`usePanelData`, though the direct `rules.run` route returned rows. Originally diagnosed as two host layers (recursive dispatch `Err`→empty + `RuleOutput` envelope never unwrapped). The build's first act — a failing Rust integration test driving `viz.query` over a seeded rule — showed `rows.len()==1`, **not 0**: against the in-process host the recursive dispatch **succeeds**; the whole `RunResult` collapsed to one JSON-blob row. So the real defect for this path is **Layer 2** (the original depth>0 `Err` was spawned-gateway-harness-specific, not structural). Fix (rules-for-widgets-scope slice 1): `viz/frame.rs::result_to_rows` unwraps the rules envelope FIRST via `unwrap_rule_envelope`, generically by SHAPE (never by tool id — CLAUDE §10 held) — full `RunResult`→recurse into `output`; bare `RuleOutput` `kind`-discriminated (`scalar`→value recursed, `grid`→shared `columnar_rows` zip, `findings`/`nothing`→empty); mirrored client-side in `useSource.ts`. No dispatcher change needed. Regression: the un-skipped `templateView.gateway.test.tsx` RULES case + host `rules_target_{scalar_array,denied_without_run_cap,workspace_isolation}` + unit `viz::frame::rule_run_result_*`. Lesson: a "shipped" picker is not a shipped render path — and when a two-layer diagnosis names a root cause, reproduce it with a failing test before writing the fix; here Layer 1 didn't reproduce in-process and Layer 2 was the whole story | **fixed** | [frontend/rules-as-source-render-path-empty.md](frontend/rules-as-source-render-path-empty.md) |
| 2026-07-09 | frontend | While an agent run is active in the dock, navigating the app "hangs" — Flows/Rules/Dashboards REST calls sit pending until the run finishes, then pop in at once (reads as "the agent blocks the node"). NOT a blocking invoke (the dock enqueues + the `agent_reactor` drives detached), NOT the store session mutex (per-query), NOT the job scan (indexed). Root cause, live-verified: the gateway is plain HTTP/1.1 (`axum::serve`, no TLS; browsers refuse cleartext h2) and **every live feed is its own `EventSource`** — run stream, dock+collab channel streams, one series stream PER dashboard cell (`useSeries.ts`'s deferred sharing), flows/telemetry/insights — so a handful of live views holds exactly **6/6** of the browser's per-origin connection slots (measured: `ss -tn '( dport = :8080 )'` = 6, same sockets 5s later, lastrcv ~6.8s = SSE frames) and every new `fetch` queues behind them. Fix BUILT (2026-07-09): the **unified event stream** — one multiplexed SSE connection per session (`GET /events/stream` + `POST /events/{sid}/{subscribe,unsubscribe}`), every feed a cap-re-checked subject (the gateway reuses each dedicated route's `lb_host::watch_*` gate, never re-implements it), client hub (`ui/src/lib/events/hub.ts`) holding ONE `EventSource` with refcounted subject dedupe + reconnect re-subscribe; all `*.stream.ts` openers became thin adapters (signatures unchanged). TLS+h2 alone rejected (browsers won't h2c → plain-HTTP dev/edge keeps the cap). Proven: `events_stream_test.rs` (6/6 — two subjects on one connection, parity vs `/bus/stream`, cap-deny + ws-isolation opaque error frames) + `hub.test.ts` (5/5 — exactly one EventSource across N subscribers, dedupe, refcount, reconnect). Remaining = the manual browser `ss < 6` symptom check (needs a running dev node). Lesson: each `EventSource` permanently spends 1 of ~6 HTTP/1.1 slots and the failure presents ELSEWHERE (unrelated REST hangs) — count established sockets before blaming the server | **fixed (verify pending)** | [frontend/agent-dock-blocks-navigation-sse-pool-exhaustion.md](frontend/agent-dock-blocks-navigation-sse-pool-exhaustion.md) |
| 2026-07-08 | reminders/frontend | The Reminders page loaded by deep link and every `reminder.*` verb worked, but the **Reminders entry never appeared in the sidebar** for a member/dev-login (Rules & Flows, its Automation-group siblings, showed fine). Root cause: the rail gate `hasCap(caps, CAP.reminderList)` (`ui/src/features/routing/allowed.ts`, `CAP.reminderList="mcp:reminder.list:call"`) is an EXACT `caps.includes(cap)` — it does NOT expand the `mcp:*.list:call` wildcard the member token carries. The built-in member role (`crates/host/src/authz/builtin_roles.rs`, base of `dev_claims`) spells out `rules.list`/`flows.list`/`datasource.list`/`insight.list` explicitly but had only `mcp:reminder.fire:call` for reminders — so the wildcard authorized the verb yet the rail filtered the entry out. Fix: added `"mcp:reminder.list:call"` to the member role next to `reminder.fire` (needs node rebuild + restart + re-login to re-mint). Same class as the dashboard-delete `isAdmin`-gate bug below: a frontend cap-gate keyed on a concrete cap can't see a wildcard grant. Lesson: any new core surface with a nav gate must add its concrete `*.list` cap to the member role the way rules/flows/datasources do — the wildcard is invisible to the literal `hasCap` | **fixed** | [reminders/reminders-nav-missing-list-cap.md](reminders/reminders-nav-missing-list-cap.md) |
| 2026-07-08 | desktop | The `full`-feature desktop binary boots cleanly (`full: seeded 38 core skills` + `full: loopback gateway on http://127.0.0.1:8800`) then dies in milliseconds with `symbol lookup error: /snap/core20/current/lib/x86_64-linux-gnu/libpthread.so.0: undefined symbol: __libc_pthread_init, version GLIBC_PRIVATE`. NOT a shell/gateway bug — the Rust boot completes; the crash is during GTK/webkit2gtk window init, where a webkit transitive dep drags in snap `core20`'s OLD glibc-2.31 `libpthread` (separate lib) into a process built against modern glibc (≥2.34, where `libpthread` is a stub in libc) → `GLIBC_PRIVATE` version mismatch. Only reproduces on a dev box with snap runtime pollution; the Ubuntu 22.04 build container has no snap and `make -C desktop smoke-full` runs clean. Fix: none in code — the container smoke is canonical; on a snap host, the non-windowed integration test (`ui/src-tauri/tests/full_loopback_test.rs` — `boot_full` + reqwest login + `POST /mcp/call`, no display) is the portable proof and is unaffected. Lesson: a windowed-binary smoke inherits the host's library-graph pathologies; keep a non-windowed integration test over the SAME in-process gateway for portability | **fixed** | [desktop/full-binary-snap-libpthread-crash-at-window-init.md](desktop/full-binary-snap-libpthread-crash-at-window-init.md) |
| 2026-07-08 | frontend | Flows debug drawer renders "Debug panel unavailable (source file missing from repo)" — the real `DebugPanel.tsx` was committed-by-reference in `9260f1a` but the repo-root `.gitignore`'s **bare `debug` pattern** (meant for Cargo output) silently swallowed the whole `ui/src/features/flows/debug/` source dir; the session tested the working copy (where the file existed) and never verified the committed tree. Fix: anchored the ignore to `/rust/target/debug/` and rebuilt the panel (useDebugStream SSE ring + type-aware DebugMessageRow + per-node filter) as the right dock's Debug tab (flow-ui-polish). Guard: `git check-ignore` on the path now exits non-zero; the unit suite imports the real panel so a re-swallow breaks the next clean checkout. Lesson: verify the COMMIT (`git show --stat`), not the working tree; never leave a bare directory-name ignore in a monorepo root | **fixed** | [frontend/flows-debug-panel-swallowed-by-bare-gitignore-pattern.md](frontend/flows-debug-panel-swallowed-by-bare-gitignore-pattern.md) |
| 2026-07-08 | desktop | First `make windows-executable` (new cargo-xwin cross lane) dies mid-crate-download with `Permission denied` on `/usr/local/cargo/registry/...` — the shared `lb-cargo` volume held root-owned files from an earlier container run while the build runs `--user $(id -u)`. Fix: one-off `chmod -R a+rwX` on the volume (or `make clean`) | **fixed** | [desktop/lb-cargo-volume-permission-denied.md](desktop/lb-cargo-volume-permission-denied.md) |
| 2026-07-08 | desktop | Every `tauri build` of the shell fails E0063: `lb_auth::Claims` grew `constraint`/`run_id` but the shell's demo-session mint (`ui/src-tauri/src/state.rs`) wasn't updated — the shell has its own lockfile and isn't in the `rust/` workspace test run, so the drift surfaced only at packaging time. Fix: init both to `None`; guard: `cargo test -p lazybones-shell` after `lb-auth` type changes | **fixed** | [desktop/shell-claims-initializer-drift.md](desktop/shell-claims-initializer-drift.md) |
| 2026-07-08 | frontend | Admin opens a dashboard's delete affordance, the destructive-confirm dialog runs, they click Delete — the dashboard stays in the roster, no visible change (reported as "I can't seem to be able to delete a dashboard from the UI"). Root cause: `DashboardRoster` shows delete for any caller holding ANY admin cap (`isAdmin(caps)`), for every dashboard regardless of owner, but `dashboard_delete` enforced owner-only with no admin override (same convention as save/share/panel/nav/assets) — a non-owner admin's click ran the confirm dialog then hit an opaque `Denied`. Fix: new `mcp:dashboard.delete_any:call` cap, checked as a second gate in `dashboard_delete` only when the caller isn't the owner (save/share stay strictly owner-only); granted to the dev-login admin set; registered in the host tool catalog. Regression: `dashboard_test::delete_any_cap_lets_a_non_owner_admin_delete`, `credentials::tests::dev_login_carries_dashboard_delete_any`. Lesson: a role-flavored UI gate (`isAdmin`) over a capability-first backend can show an affordance the host will still deny — the fix is a real capability, not a UI-only patch | **fixed** | [frontend/dashboard-delete-silently-denied-for-non-owner-admin.md](frontend/dashboard-delete-silently-denied-for-non-owner-admin.md) |
| 2026-07-08 | external-agent | A dock `extension-builder` run ("write me a hello world extension, build/load/e2e-test it") burned ~10 min doing **30+ `/bin/bash` calls and ZERO node MCP calls** — `make dev` (×3), `cargo build --release` on the repo, `chmod` its read-only tree, then a hand-written `Cargo.toml`/`extension.toml`/`build.sh` and a failing `cargo build --target wasm32-wasip2` — then hit the 15-min wall reap. Root cause: the wrappers' MCP config points the agent's MCP-server child at the shim by the bare name `lb-mcp-shim` (PATH), but on a `make dev`/`cargo run` node the shim is a **sibling binary in the target dir, not on PATH**, and `register()` never set `with_shim_bin` — so the MCP child failed to spawn, the agent got **no host tools**, and fell back to authoring by hand in its own shell → flail → stall. Contributing: the external run was bounded **only by wall-time** (the in-house loop self-bounds at `MAX_STEPS`; a flailing subprocess had nothing to reap it early), and the authoring skill didn't fence "never `make dev`/`cargo build` the repo" loudly enough to survive into an external run. Fixes: (1) `register` resolves the shim next to the node binary (`resolve_shim_bin()` → `<current_exe_dir>/lb-mcp-shim`, PATH fallback) so the bridge works on a dev node; (2) **no-progress ceiling** — a watchdog on the `RunEvent` stream in `AcpRuntime::run` reaps a run silent for 90s (`NO_PROGRESS_CEILING`), dropping the `drive` future (reaping the subprocess, same seam as the wall), closing the wall-time-only gap `run-lifecycle-scope.md` flags; (3) a "STOP before any shell command" fence at the top of the extension-authoring skill mandating the devkit MCP flow. **Then hardened to PAUSE-AND-ASK** (`agent-stall-pause-and-ask-session.md`): the stall now `suspend`s the run (resumable) + returns `AgentError::Stalled`; the worker posts a durable `kind:"agent_stalled"` item and the dock renders "Keep going" (resume_run) / "Stop" (stop_run) — the run is no longer a dead end, the user decides. Regression: `no_progress_test.rs` (Suspended + Stalled), `channel_agent_worker_test::a_stalled_run_posts_an_actionable_agent_stalled_prompt...`, UI `pendingRun`/`DockRunStatus` tests. Not-a-bug: the wall reap itself is supervision working; the defect was everything upstream that made the run need reaping. Lesson: a bridge whose shim isn't reachable degrades silently to "agent has no tools" — and a subprocess bounded only by wall-time flails for the full ceiling; both need a fast, honest, distinct failure | **fixed** | [external-agent/agent-flails-in-shell-then-stalls.md](external-agent/agent-flails-in-shell-then-stalls.md) |
| 2026-07-08 | extensions/authz | A freshly published extension's own tools (`energy-dashboard.ping`, even the boot-loaded `hello.echo`) return `denied` over `POST /mcp/call` for the dev-login admin, and never appear in `tools.catalog`. Three stacked gaps: (1) `ext_publish`→`install_extension` persists only the OUT-bound grant (`requested ∩ admin_approved`) — what the EXTENSION may call — and never grants any CALLER `mcp:<id>.<tool>:call`; the shipped `proof-panel` tools work only because they're hardcoded into `member_caps`, a path a freshly published extension doesn't have (and can't, rule 10). (2) `grants.assign` can't rescue it (no-widening: no principal holds the cap to grant). (3) `dev_claims` mints the token from a STATIC `member_caps()` and never folds the durable grants store, so even an `lb_authz::grant_assign` wouldn't change the dev token's effective caps. Fix landed here: the scaffold `[ui] scope` own-tool name bug — `devkit/scaffold.rs::tool_name` did `id.replace('-', ".")` producing `energy.dashboard.ping` while the runtime resolves `<ext>.<tool>` by `split_once('.')` with the id verbatim (`energy-dashboard.ping`); now uses the id verbatim. The cap-grant gap is OPEN: the UI/data-dashboard E2E (G5a) is unaffected (the page's data path is `bridge.call("federation.query")`, whose cap IS in `member_caps`), but the WASM-tool E2E (G5b) needs it — two viable fixes recorded (auto-grant on publish, fold grants into dev_claims). Lesson: a publish that approves out-bound authority but never in-bound callability is half an approval; the built-ins hide it because their tool caps are hand-listed | **open** | [extensions/publish-does-not-grant-tool-call-caps.md](extensions/publish-does-not-grant-tool-call-caps.md) |
| 2026-07-07 | frontend | The panel editor's Field section exposes options the renderer never reads — the user's "overwhelming and half-broken" report. Audit against every renderer classified each option LIVE (observable render change) vs DEAD (stored + round-tripped, zero visible effect). Headline: ~half the `timeseries` Field-tab options are DEAD (`mappings`, `links`, `custom.lineInterpolation`/`gradientMode`/`showPoints`/`spanNulls`/`axisPlacement`/`stacking.mode`/`thresholdsStyle.mode`) and all `table` per-column `custom.*` (width/align/cell-type/filter); the single-stat family is faithful. Root cause: the editor-parity phase registered Grafana's full surface to complete the EDITOR but the matching RENDER work only landed for the standard bridge + a few graph styles, and `registryRoundTrip.test.ts` checks only the de/serializer, not the renderer — so the gap was invisible to the green suite. Phase A landed: a real-gateway baseline test (`fieldTabBaseline.gateway.test.tsx`, 24 green) sets each option through the Field tab's own `writeOption` path and asserts a specific observable for LIVE vs byte-identical output for DEAD — it IS the contract for the simplify step. Phase B (the fix) is the named follow-up: collapse the Field tab per-view to what works (implement the dead, or remove them from the per-view registry so the editor never offers what the renderer won't draw). Lesson: a registry filled to "complete the editor" with render paths as unnamed follow-ups is how options silently go dead; an option registered in `defs/*` needs a render assertion or it can dodge coverage | **open** | [frontend/field-tab-options-that-do-nothing.md](frontend/field-tab-options-that-do-nothing.md) |
| 2026-07-06 | agent | A dock-posted GenUI widget preview rendered as `GenUiView`'s invalid/draft state: the run was healthy (catalog → proven `federation.query` → `channel.post` ✓) but the model emitted a wrong IR dialect (`type` for `component`, no per-component `id`, no `ir.v`, no `surface`) and NOTHING validated a `rich_result` at post time — `dashboard.save`'s loud `check_genui_cells` (which had visibly let the model self-correct) never runs on a conversation preview. Fix: new `channel/genui_check.rs` gates every `channel::post` (after authorize, before persist) with the SAME validator, extracted as `dashboard::genui::check_genui_block`; tightened to mirror the TS validator (per-component `id` must repeat the map key) with fix-naming messages; new `ChannelError::BadInput` → MCP `ToolError::BadInput` (the agent-loop feedback) / HTTP 400; skill doc gained a "Common IR mistakes" block. Regression = 5 `genui_check` units + `channel_agent_worker_test::a_malformed_genui_rich_result_is_rejected_and_the_corrected_repost_lands` (wrong dialect rejected+fed back, corrected repost is the ONLY genui item that lands). Lesson: a preview path that skips the save path skips its validator too — every write seam a renderer trusts needs the gate, and the error must name the fix, not just the defect | **fixed** | [agent/genui-preview-posts-wrong-ir-dialect-renders-broken.md](agent/genui-preview-posts-wrong-ir-dialect-renders-broken.md) |
| 2026-07-06 | agent | A dock ask ("give me 5 timeseries queries…") ran a healthy 9-turn session — 16 tool calls, all ✓ — then settled `Done` answering only with the turn-1 preamble ("I'll help you create… let me explore"); the user never got the queries. Not a ceiling exit: the job record shows the final turn carried EMPTY content and no calls (GLM's whole utterance was a `<think>` block; `strip_think` correctly leaves ""). The earlier last-non-empty fallback stopped the wipe but the loop still took the bare `done` at face value, so the fallback returned the preamble. Fix: a `done` turn with empty content after tool work no longer finishes — the loop nudges ONCE (`[you stopped without an answer — write your final answer…]`, live-context only, not persisted) and re-asks; a second empty stop falls back as before. Regression = `a_bare_stop_after_tool_work_is_nudged_for_the_real_answer`. Lesson: an empty `done` after real tool work is a symptom, not a finish — a fallback can only replay what the model already said; when that's a preamble, go back to the model | **fixed** | [agent/run-finished-empty-after-tool-work-answers-with-preamble.md](agent/run-finished-empty-after-tool-work-answers-with-preamble.md) |
| 2026-07-06 | datasources | `federation.query` still took 3.1 s on the demo 4-table JOIN (~956k rows) AFTER statement pushdown shipped and its structural EXPLAIN test passed — vs 0.41 s in the `sqlite3` CLI for the SAME unparsed `base_sql` with an IDENTICAL `EXPLAIN QUERY PLAN`. Two stacked causes: (1) the first retest hit a **stale supervised sidecar** (spawned before the rebuild; `/proc/<pid>/exe` → `(deleted)` — native sidecars never hot-reload); (2) after restart, the real cause: the sidecar is a dev-profile build and `libsqlite3-sys` compiles bundled `sqlite3.c` at the profile's opt-level, so SQLite itself ran at `-O0` — ~8× slower on the scan-heavy join, perfectly masking the working pushdown. Fix: `[profile.dev.package.libsqlite3-sys] opt-level = 3` in `rust/Cargo.toml`; demo query now 0.43 s through the full federated path. No timing regression test (flaky by construction); functional guarantees stay pinned by the pushdown suite. Lesson: cargo profiles apply to `cc`-built C too — when "the fix shipped but it's still slow", run the same statement through the same library build before blaming the new code, and check `/proc/<pid>/exe` for `(deleted)` first | **fixed** | [datasources/pushdown-shipped-but-still-slow-debug-sqlite.md](datasources/pushdown-shipped-but-still-slow-debug-sqlite.md) |
| 2026-07-06 | datasources | `federation.query` rejected legit SQL with `rejected sql: only SELECT/WITH allowed` when the statement carried a leading `--`/`/* */` comment (reported as "JOIN isn't allowed" — the failing statement happened to contain a JOIN; JOINs were fine, live-verified 80 rows). Root cause: `validate_select_host` took the first whitespace token as the leader, so the comment was the "leader" — the host defense-in-depth gate was STRICTER than the sidecar's authoritative parser on valid input. Fix: strip leading line/block comments before the leader check (a comment still can't hide a write — the post-strip leader is still gated; comment-only/unterminated input still rejected). Regression = `federation::validate::tests::skips_leading_comments_but_still_gates_the_real_leader`. Lesson: a keyword pre-gate in front of a real parser must never reject input the parser accepts — strip trivia first | **fixed** | [datasources/host-select-gate-rejects-leading-comment.md](datasources/host-select-gate-rejects-leading-comment.md) |
| 2026-07-05 | frontend/styles | The data-studio-10x CODE-ONLY session landed six new files (`OpenViewMenu.tsx`, `WorkbenchTab.tsx`, `panes/BuilderTabPane.tsx`, `BuilderPane.tsx`, `BuilderToolbar.tsx`) that reintroduced bare `rounded` (banned by the radius-scale guard shipped 2026-07-04); the guard red-ed the whole UI unit suite (705 → 704). Same pattern as the earlier insights-UI gap — a new feature folder didn't learn the convention. Fix: each offender mapped to the closest neighboring stop (`rounded-md` for menu-item rows matching the sibling caret menu; `rounded-sm` for tight chips/icon-only buttons). Regression = the guard test (it IS the prevention). Lesson: a source-level convention guard catches new feature folders that didn't learn the convention — fix is applying the convention, not loosening the guard; a new feature folder should run `pnpm test src/styles/radius-scale.guard.test.ts` before declaring code-done | **fixed** | [frontend/data-studio-10x-bare-rounded-radius-guard.md](frontend/data-studio-10x-bare-rounded-radius-guard.md) |
| 2026-07-05 | federation | A handful of failed `federation.query` calls took the datasource dark for the whole session — `supervisor: restart budget exhausted after 5 restarts` on every further call. Root cause: `native/call.rs` treated `SupervisorError::Child(_)` (the child's ordinary error REPLY — a failed SQL query) as "the child died" and restarted a healthy child per failed query; five failures exhausted the budget. Fix: only `Transport(_)` (no reply) is a fault; a `Child` error surfaces as the tool error it is; the sidecar additionally fences each call in its own task so a real panic becomes an error reply; a bare `COUNT(*)`'s zero-column-scan internal error (upstream datafusion-table-providers bug) is rewritten to a steer (`count a concrete column, e.g. COUNT(id)`). Lesson: "error reply" and "dead child" are different failure planes — conflating them turns callers' mistakes into infrastructure outages | **fixed** | [federation/error-reply-treated-as-crash-restart-budget-exhausted.md](federation/error-reply-treated-as-crash-restart-budget-exhausted.md) |
| 2026-07-05 | federation | Even with the steering rejection, live models kept probing `information_schema.tables` first (the strongest prior an OpenAI-schooled model has) and burned turns on the rejection. Decision: answer the probe instead of fighting it — the sidecar now synthesizes `information_schema.tables`/`columns` per query as read-only in-memory views built from the source's REAL catalog (DataFusion's built-in info-schema would be empty: it only reflects registered tables); `pg_catalog` and unknown views still steer. Host gate updated to let the two views through. Supersedes the steering half of the earlier entry | **fixed** | [federation/information-schema-now-answered-read-only.md](federation/information-schema-now-answered-read-only.md) |
| 2026-07-05 | agent | The agent answered "dashboard created!" but the user's list didn't show it and `dashboard.get` was `denied` — the record was real, `private`, and owned by the DERIVED `agent:session` sub (the loop's audit identity), i.e. invisible to everyone. Fix: `Principal.derive` now records the delegation root (`delegator`, preserved across nested derives like `constraint`); `owner_sub()` = delegator-or-sub; dashboard + panel ownership stamps and owner/visibility walls read `owner_sub()` — an agent-created record belongs to the human who asked while audit keeps the `agent:*` actor. Caps/isolation unchanged. Lesson: delegation needs TWO identities — who acted (audit) and who it was for (ownership); collapsing them makes the agent's work vanish | **fixed** | [agent/agent-created-dashboard-invisible-derived-owner.md](agent/agent-created-dashboard-invisible-derived-owner.md) |
| 2026-07-05 | agent | Live GLM retried the IDENTICAL rejected tool call 3–5 turns straight — tool errors were fed back but had no effect. Root cause: the OpenAI-compat adapter sent results as ORPHAN `role:"tool"` messages (no assistant `tool_calls` echo; history summaries had no id at all) — measured live, the model half-ignores orphans and loses call context (guessed datasource names). Fix: `CallOutcome`/`ToolResult` now carry the proposed call's name+input (rehydrate pairs them from `ToolCallProposed`); the adapter emits the conformant assistant-echo → keyed-result shape and re-rolls history tool summaries as user text. Regression = real-HTTP body assert in `openai_compat_test`. Lesson: feeding an error back isn't enough — it must arrive in the shape the model was trained to anchor on | **fixed** | [agent/tool-errors-ignored-orphan-tool-messages.md](agent/tool-errors-ignored-orphan-tool-messages.md) |
| 2026-07-05 | agent | `dashboard.save` failed five turns straight on `cells: invalid type: string` (the model sent the array JSON-encoded), then once on `missing field widget_type`; `dashboard.share` failed on a stringly `now`. Three ergonomic gaps at one seam: save/share had NO arg schema (name-only rows), the bridge decoded strictly with unsteering errors, and `Cell.widget_type` was the only version-specific field without `#[serde(default)]` (a v3 `view` cell — exactly what the catalog teaches — couldn't deserialize). Fix: real descriptors for `dashboard.save`/`dashboard.share`; `typed_arg`/`u64_arg` leniency (stringified JSON / numeric strings) with validators still authoritative; `widget_type` defaulted. Lesson: every write verb an AI persona leans on needs a schema, and serde strictness at an AI seam is a five-turn tax | **fixed** | [agent/dashboard-save-cells-sent-as-json-string.md](agent/dashboard-save-cells-sent-as-json-string.md) |
| 2026-07-05 | agent | Every live widget-builder run died at the run loop's 8-turn ceiling (honest `[stopped at its 8-turn ceiling …]` note, but never a finished widget) — even a follow-up run pointed straight at the datasource. Root cause: `MAX_STEPS = 8` predates builder personas; the honest build path (orient → `federation.schema` → probe queries with one real-error retry → `viz.query` → `dashboard.save`) measured 10–14 model turns live. Fix: `MAX_STEPS` 8 → 16 (comment records the measurement); the per-workspace/persona ceiling stays the recorded scope follow-up. Lesson: a global turn budget sized for Q&A personas silently caps out every persona whose job is a pipeline | **fixed** | [agent/run-ceiling-too-low-for-builder-personas.md](agent/run-ceiling-too-low-for-builder-personas.md) |
| 2026-07-05 | agent+federation | The widget-builder run probed `SELECT … FROM information_schema.tables` via `federation.query`, got a cryptic DataFusion "table not found" (catalog schemas are unreachable by design), then GUESSED a table name (`meter_readings`) and failed again. Root cause: no steering — the planner never registers `information_schema`/`pg_catalog` so the probe dies deep in DataFusion — AND the actual discovery verb `federation.schema` was advertised name-only (no arg schema, a gap the round-1 catalog fix left), so the model couldn't form the call. Fix: both validators (host gate `federation/validate.rs`, verified live, + sidecar `extensions/federation/src/validate.rs`, defense in depth) now reject catalog-schema SQL with a message naming `federation.schema` and its `{source, table?}` args; `federation.schema` got a real descriptor (`federation/schema.rs` → `tools/descriptor.rs`). Verified live: the retest led with `datasource.list → federation.schema`, zero probes, zero guessed tables. Regression = steering tests in both validators. Lesson: an error that can't name the right next verb trains the model to guess; every discovery verb a persona depends on needs an arg schema, not just a name | **fixed** | [agent/federation-information-schema-probe-cryptic-plan-error.md](agent/federation-information-schema-probe-cryptic-plan-error.md) |
| 2026-07-05 | agent | The dock rendered an `_(empty)_` agent answer for "add a widget…" (and the user couldn't see which tools a run called — only a transient "calling X…" line that vanished on completion). Root cause: `run.rs` set the run answer to the LAST turn's content unconditionally — a final `done` turn with empty content (GLM after think-strip) or a MAX_STEPS(8) ceiling exit mid-tool-work (multi-step widget builds burn all 8 turns on text-less tool turns) wiped the answer to `""`, and the ceiling exit settled silently as `Done`. Fix: `last_content` only updates on non-empty content; a ceiling exit appends an honest `[stopped at its 8-turn ceiling …]` note; the dock now renders the run's tool calls as a live ✓/✗/spinner list that STAYS after the run settles, and "Copy for AI" appends the latest run's captured calls. Regression = `agent_answer_fallback_test` (rust) + `DockRunStatus`/`exportTranscript` tests (ui). Lesson: "answer = last turn's content" conflates most-recent with most-meaningful, and a resource-ceiling exit must be visible in the output itself | **fixed** | [agent/run-answer-empty-last-turn-content-overwrites.md](agent/run-answer-empty-last-turn-content-overwrites.md) |
| 2026-07-05 | agent | A `builtin.widget-builder` run (which `extends` data-analyst) reported only THREE tools — `dashboard.catalog`, `dashboard.pin`, `federation.query` — and claimed `datasource.list` "is not available". The persona layer was INNOCENT (`resolve_effective` unions the `extends` closure, `narrow_tools` globs correctly — proven by a test against the unfixed code). Root cause, two stacked gaps upstream: (1) `tools.catalog` only enumerated the ~11 verbs with guided palette descriptors (`host_descriptors()`), so `reachable_tools` could never advertise `datasource.*`, `store.query`, `series.*`, `viz.query`, `flows.*`, … regardless of caps or persona — intersect with widget-builder's globs and exactly 3 tools remain; (2) the "authoritative" `system/catalog.rs::HOST_TOOLS` inventory had itself drifted — whole dispatched families missing — because its coverage test iterated a hand-copied, stale prefix list. Fix: `tools.catalog` now also serves the full host-native inventory (name-only rows, filtered to `is_host_native`, gated by the SAME per-verb `authorize_tool`); added the ~70 missing `HOST_TOOLS` rows; the dispatch families became shared consts (`HOST_NATIVE_PREFIXES`/`HOST_NATIVE_EXACT`) that both `is_host_native` and the coverage test derive from, so the mirror cannot drift silently again. Regression = `persona_menu_full_catalog_test` (menu carries the granted inventory + persona keeps the parent surface + ungranted stays absent). Lesson: when a menu is `catalog ∩ caps ∩ persona`, starve the catalog and every downstream layer looks broken — and a "covers everything" test that iterates a hand-copied list covers nothing | **fixed** | [agent/persona-menu-missing-tools-catalog-descriptor-only.md](agent/persona-menu-missing-tools-catalog-descriptor-only.md) |
| 2026-07-05 | agent | The in-house agent (GLM-4.6) completed turns but behaved badly — asked "what's your datasource name?" in prose instead of calling `datasource.list`, and leaked raw `</think>` tags. Root cause (two defects at the model boundary): (1) tool parameter schemas were dropped end-to-end — `ToolDescriptor.input_schema` exists but `menu.rs`→`AllowedTool`→`ToolSchema` had no field for it and `openai_compat` hardcoded `parameters:{type:object}`, so every tool looked argument-less and the model asked the user rather than forming a call; (2) GLM `<think>…</think>` reasoning was passed through verbatim. Fix: thread `input_schema` catalog→`function.parameters` (degrade to `{}` only when truly schemaless) + a `strip_think` at the adapter boundary. Proven LIVE against Z.AI: with a real schema GLM proposed the tool call instead of prose. Regression = `openai_compat_test` (real-HTTP body assert) + 5 `strip_think` units + the network-gated live test. Lesson: a tool with an empty param schema is worse than no tool — the model stalls into asking; thread the whole contract and assert the provider request body, not just the response | **fixed** | [agent/tool-schema-dropped-so-model-asks-in-prose.md](agent/tool-schema-dropped-so-model-asks-in-prose.md) |
| 2026-07-05 | agent | The In-house agent card showed "model key: set ✓" but still answered the unconfigured placeholder — a workspace's sealed key for its pick of a read-only built-in never reached a run. Root cause: "Set model key" writes the sealed PATH onto `agent.config.model_endpoint.api_key_secret`, but `resolve_workspace_model` read the key from the DEFINITION's endpoint (the built-in `agents.toml` record, which carries no secret) and never overlaid the config's selection layer — so `secret=None` → env fallback (unset) → an unauthenticated adapter. The feature was wired on the write side and dead on the read side. Fix: a `overlay_config_endpoint` (config field wins, else inherit the definition) applied in the resolver before key/model build. Regression = `agent_active_model_test::a_builtin_pick_resolves_its_sealed_key_from_agent_config` + 4 overlay unit tests. Lesson: when a selection layer is designed to override a read-only preset, the overlay must exist on the READ side too — "set ✓" is only honest if the resolver reads the layer the writer wrote | **fixed** | [agent/config-sealed-key-not-overlaid-onto-builtin-pick.md](agent/config-sealed-key-not-overlaid-onto-builtin-pick.md) |
| 2026-07-05 | frontend/motion+theme | Every `RulesView.gateway.test.tsx` case (11) — and the two CommandPalette gateway suites logged 2026-07-04 — crashed at render with `useTheme must be used within ThemeProvider`, thrown from `<Reveal>`: `useMotionPref` called the THROWING `useTheme()`, so any motion-wrapped page hard-required the app-shell provider; gateway tests mount surfaces bare. Fix: `useMotionPref` reads `useThemeOptional()?.theme ?? DEFAULT_THEME` (the `CodeEditor` pattern) — motion degrades to the default preference, never a crash. Regression: `src/lib/motion/useMotionPref.test.tsx` (probe + `<Reveal>` with no provider) + the 11 rules and 8 palette gateway tests back green. Lesson: shared `lib/*` seams must not hard-require an app-shell provider — optional context read + default | **fixed** | [frontend/reveal-crashes-outside-theme-provider.md](frontend/reveal-crashes-outside-theme-provider.md) |
| 2026-07-05 | insights/ui+styles | Insights UI used bare `rounded` on three `<code>` chips (InsightDetail/InsightsList), failing the radius-scale guard and red-ing the whole UI unit suite (631 tests). The radius bug shipped 2026-07-04 with a repo-wide `rounded`→`rounded-md` sweep + a source guard (`radius-scale.guard.test.ts`); the insights feature folder was added after the sweep and reintroduced bare `rounded`. Fix: the three chips → `rounded-md` (deliberate `rounded-full` pills untouched). Regression = the guard test (it IS the prevention). Lesson: a source-level convention guard catches new feature folders that didn't learn the convention — fix is applying the convention, not loosening the guard | **fixed** | [insights/insights-ui-used-bare-rounded.md](insights/insights-ui-used-bare-rounded.md) |
| 2026-07-05 | insights/skills | `cargo test --workspace` aborted at `core_skills_test::the_default_set_is_the_read_only_core_skills_with_an_env_override` (asserted `DEFAULT_CORE_SKILLS == [3 fixed skills]` but the constant is the 21-skill persona-grounding set incl. `core.insights`), so none of the alphabetically-later insights test binaries ran. Stale assertion, not a stale constant — the persona-grounding session grew the set and forgot the test. Fix: invariant + canonical-member spot-checks (non-empty, all `core.`, key members present, `core.secrets` absent) instead of brittle whole-array equality. Regression = the renamed test. Lesson: a grown-by-design set needs an invariant assertion, not an exact-array equality | **fixed** | [insights/core-skills-default-set-assertion-stale.md](insights/core-skills-default-set-assertion-stale.md) |
| 2026-07-05 | insights/notify+prefs | The `insight_notifications` prefs axis (the per-member kill switch) silently did nothing — set false, deliveries still posted. `lb_prefs` is SCHEMAFULL; the new axis was in the Rust `Prefs` struct + the host read path but missing from BOTH `PREFS_COLUMNS` and the `DEFINE FIELD` lists in `prefs/src/store/schema.rs`, so SurrealDB dropped the write and the projection didn't even select it on read-back (round-tripped `None`). Fix: add `insight_notifications TYPE option<bool>` to both tables + the column projection. Regression = `insights_test::member_kill_switch_off_skips_all_deliveries`. Lesson: a new nullable prefs axis MUST be added in three places (struct + columns + DEFINE FIELD); SCHEMAFULL silently drops what isn't declared | **fixed** | [insights/prefs-insight-notifications-axis-not-persisted.md](insights/prefs-insight-notifications-axis-not-persisted.md) |
| 2026-07-05 | insights/occurrences | `insight.occurrences` returned `seq` as a ULID string, not the per-insight monotone `u64` — newest-first ordering broke. Root cause: `Occurrence.seq` (u64) collided with `lb_store::capped_insert`'s OWN injected `seq` (a ULID it owns for FIFO eviction) — same field name, two concepts, the ULID won on decode. Fix: `#[serde(rename = "oseq")]` on the Rust field — wire/store name `oseq`, `capped_insert`'s `seq` ignored on decode. Regression = `insights_test::ring_cap_evicts_oldest_but_count_is_lifetime`. Lesson: a host record stored through a store primitive that injects its own fields must not share field names with it — dodge at the serde boundary, keep the Rust domain name readable | **fixed** | [insights/capped-insert-seq-collides-with-occurrence-seq.md](insights/capped-insert-seq-collides-with-occurrence-seq.md) |
| 2026-07-05 | insights/store | `insight.list` + the digest reactor's `all_notify` returned zero rows (or undecodable rows) even though `insight.get` round-tripped the record fine. Root cause: `lb_store::scan` (the id-cursor paging API used for whole-table walks) returns the WHOLE record `{data:{…}, rev}`, while `read`/`list` select the inner `data` field directly — `scan_all` passed `scan`'s rows straight to `serde` so every row was an envelope the record shape didn't match. Fix: `scan_all` unwraps `data` before returning each row. Regression = `insights_test::list_in_one_workspace…` + `::digest_reactor_is_idempotent_on_rerun`. Lesson: the store has two read shapes (record vs inner-data); a helper that walks `scan` pages must unwrap to the same shape `read` returns | **fixed** | [insights/store-scan-returns-data-envelope-not-the-record.md](insights/store-scan-returns-data-envelope-not-the-record.md) |
| 2026-07-05 | frontend/caps | The agent dock's first send silently did nothing (post never landed; UI stuck on "No messages yet") for an ordinary member. Root cause: dock session ids were `dock.{slug}.{ulid}` (dotted), and the capability grammar (`crates/caps/grammar.rs`) splits a resource on BOTH `/` and `.` — so `chan/dock.ada.01H…` is 4 segments, and the member's `bus:chan/*:pub` (single `*` = ONE segment) can't match it → create-on-post `Denied`. Fix: make the id ONE segment (`dock-{slug}-{ulid}`, `-` is not a delimiter); the existing member grant then covers it, no new/wider cap. Regression: `dockId.test.ts` asserts the minted id has no `.`/`/`; the real-gateway create-on-post test passes for an ordinary member (would 403 for a dotted id). Lesson: a channel id IS a cap resource path — a UI-minted id covered by a single-`*` grant must stay one segment | **fixed** | [frontend/dock-channel-id-dotted-cap-deny.md](frontend/dock-channel-id-dotted-cap-deny.md) |
| 2026-07-05 | frontend/host | A `template`/chart/table cell bound to `{tool:"rules.run"}` renders ZERO rows through `viz.query`, even though the **direct** `rules_run` route returns the rows (verified live: `runRule→{kind:"scalar",value:[3 rows]}` ✓ vs the same rule via `viz.query`→`rows.length===0` ✗, regardless of caps). Two host layers: (1) `viz_query`'s recursive `call_tool_at_depth("rules.run",…)` returns `Err` so `dispatch_target` falls to its empty-rows arm; (2) `result_to_rows` doesn't unwrap the `RuleOutput` envelope (`{kind,value|columns+rows}`) — `ROW_KEYS` has no match for `output`, so even with Layer 1 fixed a RunResult would collapse to one JSON-blob row. Surfaced during the render-template-inprocess scope (its headline "Rules work for free" is gated on this); the new in-process `TemplateView` is source-agnostic and correct (series/SQL renders 3 real rows) so it needs NO template-side change once the host gap is fixed. Out of that scope's "no pipeline change" boundary — a one-line `ROW_KEYS += "output"` was tried and reverted (output is an envelope object, not a bare array → no-op). The rules-as-source PICKER shipped (`rulesSource.gateway.test.tsx`); the RENDER path was never driven against the real gateway, which is why this hid. Fix (separate scope): trace the recursive-dispatch Err + teach `result_to_rows` the RuleOutput envelope (mirror in client `useSource.ts`). Regression = the `it.skip` rules case in `templateView.gateway.test.tsx` un-skips green | **open** | [frontend/rules-as-source-render-path-empty.md](frontend/rules-as-source-render-path-empty.md) |
| 2026-07-04 | frontend | `pnpm test:gateway src/features/channel/palette/CommandPalette.gateway.test.tsx` (6 cases) AND `src/features/channel/palette/CommandPalette.agent.gateway.test.tsx` (2+ cases) fail fast with `Error: useTheme must be used within ThemeProvider` (from `src/lib/theme/useTheme.ts:8`), raised during the `<ChannelView>` render. PRE-EXISTING red, NOT a widget-platform Slice C regression: Slice C's only UI change is the additive `ui/src/features/channel/ResponseViewResultRender.gateway.test.tsx` (3/3 green; mounts `<MessageItem>` directly, never `<ChannelView>`), Slice C touched ZERO theme/motion/ChannelView code, AND vitest runs each test file in isolation (its own module registry) so a new test file cannot break unrelated ones — both failing files fail IDENTICALLY in isolation. The four sibling gateway tests that mount `<MessageItem>` directly are GREEN (`PinToDashboard.gateway` 4/4, `CommandPalette.reminders.gateway` 11/11, the new `ResponseViewResultRender.gateway` 3/3). Cause: in-flight motion/theme work uncommitted in the working tree before Slice C started (`git status` shows pre-existing mods to motion consumers + `panel-builder` + flows rename + `viz.phase3.gateway.test` + `data-studio-ux` + `useSource`/`useVizQuery` + `dashboard.types`) routed a `Reveal`/`useMotionPref` (a `useTheme` consumer) into a `ChannelView` path the gateway tests render WITHOUT a `<ThemeProvider>` wrapper. Surfaced during widget-platform Slice C. Fix (for the in-flight motion/theme owner): either (1) wrap `<ChannelView>` in the two failing gateway tests with the same `<ThemeProvider>` the unit-suite uses (the render path now legitimately needs it), or (2) make `Reveal`/`useMotionPref`'s `useTheme` call OPTIONAL (`useThemeOptional()` pattern, debugging/frontend/ext-widget-standalone-mount-throws-no-dashboard-cache-provider.md) if `Reveal` is supposed to degrade outside a provider. Lesson: a primitive upgraded to provider-required becomes a NEW transitive requirement on every component that mounts it; the gateway suite (full `<ChannelView>` without the App shell) is the sentinel. RESOLVED 2026-07-05 via option (2): `useMotionPref` now reads `useThemeOptional() ?? DEFAULT_THEME` — both palette suites green again (see [frontend/reveal-crashes-outside-theme-provider.md](frontend/reveal-crashes-outside-theme-provider.md)) | **fixed** | [frontend/channel-palette-gateway-useTheme-not-in-provider.md](frontend/channel-palette-gateway-useTheme-not-in-provider.md) |
| 2026-07-04 | widgets | `cargo test -p lb-host --test panel_test` has 4 failing cases (`ref_hydrates_coexists_propagates_and_ignores_echoed_spec`, `cross_ws_ref_rejected_and_dangling_placeholders`, `delete_refused_while_in_use_unless_forced`, `dashboard_save_returns_hydrated_ref_cells`), each `BadInput("cell c1/c2: unknown view 'STALE' — call dashboard.catalog")`. PRE-EXISTING Slice A blast-radius, NOT a Slice B regression: the `panel_test` fixtures use `view:"STALE"` as a placeholder echoed spec (to prove hydration overwrites it), and Slice A's `check_view_cells` (`save.rs:48`, UNCHANGED by Slice B — `git diff --stat HEAD` shows Slice B is purely additive) rejects "STALE" before `validate_and_strip_refs` runs. `git log` shows `panel_test.rs` last touched at `de9e9a7 "added backend widgets"` (the Slice A commit), so Slice A shipped these reds. Fix (for the Slice A owner / a follow-up): test-fixture-only — change the echoed-spec placeholder from `"STALE"` to a REAL-but-DIFFERENT view from the panel's real view (e.g. `"gauge"` vs the panel's `"stat"`) so the validator passes AND the hydration-overwrite intent (`gauge≠stat`) is preserved. Slice B did NOT absorb it (out of scope; the fix needs the panel-test's intent understood to avoid masking it). Surfaced during widget-platform Slice B | **open** | [widgets/panel-test-stale-view-preexisting-slice-a-red.md](widgets/panel-test-stale-view-preexisting-slice-a-red.md) |
| 2026-07-04 | app | Unistyles 3 theme resolves to `never` (every `StyleSheet.create((t)=>…)` param + `useUnistyles().theme` untyped; `initialTheme:'dark'` rejected as `keyof never`). Two independent causes, both required: (1) the type-augmentation file was `unistyles.d.ts` NEXT TO `unistyles.ts`, so tsc treats it as that file's emit output and silently drops it from the program (`tsc --listFilesOnly` shows it absent) — rename to a distinct basename (`theme-augment.d.ts`); (2) `UnistylesThemes` is a MODULE export, so merge via `declare module 'react-native-unistyles'`, not a global `interface`. Regression guard = `pnpm typecheck` | resolved | [app/unistyles-theme-never.md](app/unistyles-theme-never.md) |
| 2026-07-04 | app | react-native-svg breaks the Vite RN-Web preview: its Fabric `*NativeComponent.js` files import native-only RN internals (`codegenNativeComponent`, `TurboModuleRegistry`) that react-native-web doesn't expose, and the RN-Web plugin's esbuild alias rewrites `codegenNativeComponent` to a non-absolute path the pre-bundler can't read → blank light page. Patching imports one-by-one just surfaces the next missing module. Fix: alias `react-native-svg`→a DOM-`<svg>` shim (`web/svg-shim/`) for the PREVIEW ONLY (device uses the real pkg); rule-9-clean (rendering shim, not a fake backend). Regression = puppeteer asserts GaugeRing draws 60 `<circle>`s | resolved | [app/rn-svg-web-preview.md](app/rn-svg-web-preview.md) |
| 2026-07-04 | build | `make android` (real device connected) builds + installs the APK fine but fails at the run step, in three successive layers once each is fixed: (1) `adb: not found` / `SDK location not found … ANDROID_HOME` — Gradle builds via `android/local.properties` `sdk.dir`, but the RN CLI shells out to `adb`/`emulator` by name for `adb reverse`+`am start` and the SDK (`~/Android/Sdk`) was never on PATH; (2) Metro won't boot — `[RepackModuleFederationPlugin] Dependency '@module-federation/enhanced' is required, but not found` (peer of Re.Pack's MF2 plugin, absent from the shell's standalone node_modules); (3) bundle `Can't resolve '@swc/helpers/_/_interop_require_wildcard'` (required by `react-native-screens`' pre-compiled source); (4) bundle `× Expected ',', got 'ident'` on `export enum VirtualViewMode` in `react-native/src/private/components/virtualview/VirtualView.js` — RN 0.86 core ships Flow `enum`s, but Re.Pack's `flow-loader` uses `flow-remove-types` (2.321.0) which only STRIPS type annotations and leaves the `enum` keyword intact (verified: re-emits `enum VirtualViewMode`), so SWC then errors; Metro's own pipeline survives only because `@react-native/babel-preset` runs `babel-plugin-transform-flow-enums` (hermes-parser-backed), which Re.Pack's SWC path has no equivalent for. Fixes: (1) `app/Makefile` resolves `ANDROID_HOME ?= $(or $(ANDROID_SDK_ROOT),$(HOME)/Android/Sdk)`, asserts `platform-tools/adb` exists, and runs `pnpm android` with `ANDROID_HOME`+`platform-tools`/`emulator` prepended to PATH (no shell-profile edit; override `make android ANDROID_HOME=/path`); (2)/(3) `pnpm add --ignore-workspace --config.minimumReleaseAge=0 @module-federation/enhanced@0.22.0 @swc/helpers` in `app/shell` — the `minimumReleaseAge=0` is REQUIRED because `--ignore-workspace` also ignores the shell's own `pnpm-workspace.yaml` (which sets it to 0), so fresh Expo-57 lockfile entries otherwise trip the supply-chain age policy; match the MF runtime version already locked (`0.22.0`); (4) a Babel `pre`-loader in `app/shell/rspack.config.mjs`, scoped to `react-native`+`@react-native` via `Repack.getModulePaths(...)`, runs `babel-plugin-syntax-hermes-parser`+`babel-plugin-transform-flow-enums` before flow-loader/SWC (all four plugins already present transitively). Verified: `make android` exit 0, app launches on `SM-A566B`, no FATAL/JS error, gateway DNS calls fire, android bundle HTTP 200 ~8.1 MB. One remaining non-fatal ⚠: optional `@react-native-masked-view/masked-view` (a `@react-navigation/elements` optional peer). Lesson: a Gradle build succeeding does NOT mean the RN run toolchain is on PATH; and `flow-remove-types` cannot lower Flow enums — an SWC-based RN 0.86 bundler needs an explicit enum transform the Babel preset otherwise hides | resolved | [build/android-run-repack-flow-enum-and-missing-deps.md](build/android-run-repack-flow-enum-and-missing-deps.md) |
| 2026-07-04 | datasources | Real-world e2e of the datasources runbook against the seeded TimescaleDB (`make dev` → `federation.query` on the `timescale` source): a bare `SELECT count(*) FROM point_reading` (and `count(1)` / `sum(1)`) returns `Internal error: Physical input schema should be the same … (physical) 1 vs (logical) 0` instead of a row count, and on retry the child panics until `supervisor: restart budget exhausted after 5 restarts`. Trigger = a **column-less aggregate**: DataFusion prunes the base-table scan to an EMPTY projection for `count(*)`, but the pushed-down `datafusion-table-providers` Postgres provider (df 53.1, providers 0.11) reports a 1-field physical schema against a 0-field logical input → df 53's new aggregate schema verifier rejects it; enabling `execution.skip_physical_aggregate_schema_check` moves the error one stage on to Arrow's `BatchCoalescer` (`Batch has 0 columns but BatchCoalescer expects 1`), so the flag is necessary-not-sufficient. Wrapping the table (`… FROM (SELECT 1 AS c FROM t)`) doesn't help — the optimizer re-prunes to empty. Only an aggregate over a REAL column works: `count(value)` → `475974`, and `GROUP BY`/`avg`/`max`/`min` all fine. Not a false-bug: DB seeded (475 974 `point_reading` rows), federation on, node fresh, time unit correct (the value/time reads render pt-001 Energy kWh on a correct 15-min axis). Interim workaround: `count(<not-null column>)`. Complete fix (deferred — its own scoped session): detect the column-less aggregate via the parsed AST and rewrite its argument to a NOT-NULL column from the provider's Arrow schema, plus the flag. Regression: `host/tests/federation_test.rs::federation_count_star_columnless_aggregate` — real `postgres:16-alpine` fixture, asserts `count(seq)`=5 then `count(*)`=5; **`#[ignore]`d fails-until-fixed dead-drop, verified fails-before** with the exact `physical 1 vs logical 0` panic. Lesson: a column-less aggregate pushdown produces an empty scan that both df's schema verifier and Arrow's coalescer reject — the fix must keep a real column in the scan, not just silence the check | **open** | [datasources/count-star-aggregate-schema-mismatch.md](datasources/count-star-aggregate-schema-mismatch.md) |
| 2026-07-04 | app | RN-web preview login showed **"Failed to fetch"** on Sign in, or the gateway 403'd the prefilled `ada`/`acme` with **"not a member of any workspace"** — the preview looked broken against a healthy node. Two layers: (1) the preview defaulted `?node=` to **8087** (the app's own `test_gateway`) but the user runs root `make dev` whose node is on **8080** — nothing on 8087 → `fetch` rejects; pointing at 8080 turned it into a real **403**. (2) The identity model keys on the **`user:<name>` principal** everywhere (token `sub`, `membership` row, `created_by`, seed `LB_SEED_USER=user:ada`), but `role/gateway/src/routes/login.rs` used the request `user` string VERBATIM — so bare `ada` was a principal literally named `ada`, a DIFFERENT identity from the seeded `user:ada`; `membership_login_resolve` found `acme` had members but not `ada` → NotAMember. It "worked" only on an empty in-memory `test_gateway` because there `ada` bootstrapped as first member (decision #3), masking the bug until the preview met a POPULATED store. Fixed by canonicalizing the handle at the login edge (`let principal = if req.user.starts_with("user:") {…} else { format!("user:{}", req.user) }`) before `user_login_check`/`membership_login_resolve`/`dev_claims`/grant-resolve/`LoginReply` — `ada` and `user:ada` now resolve to the same identity on any node; an empty node still bootstraps it. Edge normalization only; core membership/`Subject` handling unchanged, no extension branch. Follow-ons the fix exposed: preview default port+prefill moved 8087→8080 (`dev-defaults.ts` + `web/index.web.tsx`; `?node=` still overrides), and the `addMember` test harness was passing a bare `"bob"` (which wrote a `bob` roster row AND skipped the member-role grant — `membership_add`'s `bare_user()` only grants for a `user:`-prefixed sub; it "worked" only because old `login("bob")` matched the bare row) — canonicalized to `user:bob`. Regression: `identity_routes_test::login_canonicalizes_a_bare_handle_to_the_user_principal` (prefixed bootstrap, then bare handle admitted as the SAME member with canonical principal) + Playwright e2e (bare `ada` → real channels) + `pnpm test:gateway` 17/17. Twin of the session-liveness half [stale-preview-session-shows-empty](app/stale-preview-session-shows-empty.md). Lesson: normalize a convenience input to its canonical key at the single edge that admits it — a raw handle let an in-memory node (bootstraps any stranger) hide a bug a seeded node exposed | resolved | [app/bare-login-handle-not-a-member.md](app/bare-login-handle-not-a-member.md) |
| 2026-07-04 | frontend | the theme Customizer's **radius** control visibly did nothing for most of the app — cards/inputs/chips/buttons kept their corners no matter the setting; only `rounded-sm/md/lg` shadcn surfaces moved. `theme-dom.ts` writes `--radius` correctly (unit-tested), but in `globals.css` only `--radius-sm/md/lg` derived from it — Tailwind's bare `rounded` (`--radius-DEFAULT` static `0.25rem`), `rounded-xl`, `rounded-2xl` never referenced the token, so ~114 call sites were pinned to static defaults. Deeper cause found only during **live-verify**: `@import "tw-animate-css"` re-imports Tailwind's default `@theme`, which re-declares `--radius-sm/md/lg` statically into `:root` **after** our block in the compiled sheet, so custom-property resolution lost the cascade to the static `.375rem` even after we derived them. jsdom runs no Tailwind and computes no `var()`, so the whole failure was invisible to the green unit suite. Fixed in `globals.css`: (1) declare the full ladder `--radius-xs…3xl`+DEFAULT off `var(--radius)` in `@theme` (generates the `rounded-xl/2xl/3xl` utilities); (2) a **cascade-last `:root:root` override** (specificity 0,2,0) re-asserting the derived values so it beats tw-animate-css's plain-`:root` re-emission by specificity, order-independent — the load-bearing line; sm/md/lg keep shipped offsets (shadcn byte-identical, no regression), `rounded` pinned to `md`, `max(0px,…)` clamps `0rem` to square; (3) swept all bare `rounded` → `rounded-md` across 44 `.tsx`. Regression: `ui/src/styles/radius-scale.guard.test.ts` (source guard: every stop derives from `var(--radius)`, the `:root:root` override exists, no bare `rounded`) + live-verified in real Chromium (`@playwright/test` computed `borderRadius`: md 6→14px, lg 8→16px, xl 12→20px as `--radius` 0.5→1rem; `0rem`→square; `rounded-full` stays pill). Lesson: jsdom cannot verify Tailwind or `var()` — token→utility derivations must be live-verified by compiling the CSS and reading computed style in a real engine; and a dependency that re-imports Tailwind's default theme re-emits the default token values last, so overriding needs specificity, not source order | resolved | [frontend/radius-control-does-nothing.md](frontend/radius-control-does-nothing.md) |
| 2026-07-04 | app | RN-web browser preview: after logging in, creating a channel, and restarting the preview gateway (any `make -C app dev` restart), a page reload showed the Channels screen with **"No channels yet — create one below."** instead of a login prompt — the user read it as "my channels vanished." Cause: the preview `test_gateway` boots **in-memory** (no `LB_STORE_PATH` → `Store::memory`) and mints a **fresh signing key every boot** (`role/gateway/src/signing_key.rs::resolve` falls to `generate()` with no durable store), so a restart both wipes the channels AND invalidates every issued token; but the web preview persists the session in `localStorage` (`keychain.storage.web.ts`) and the shell rehydrated it via `session.restore()` — which only reads storage back and never contacts the node — so a DEAD token drove a logged-in UI. The existing 401→drop path (`request.ts`→`onAuthError`→`logout`) only fired opportunistically on a later read, and not at all during the restart window when the node is briefly down (`fetch` rejects, not 401). This is the **in-memory-preview twin** of the durable-node bug in `auth/signing-key-not-persisted-invalidates-sessions.md` (there the fix was to persist the key beside the durable store; here the node is deliberately ephemeral, so the client must validate instead). Fixed at the SDK layer: new `client.restore()` (`app/sdk/src/client/create.ts` + `session/validate.ts::probeSession`) rehydrates then probes once with `GET /workspaces`, classifying the outcome — **401** → drop (token dead); **network error** → drop when `onUnreachable:"drop"` (a throwaway preview node makes an unverifiable session worthless) or keep for a durable-node device build; **403/2xx** → node authenticated the token → keep (a cap deny is not staleness). `app/shell/src/lib/client.ts` now calls `client.restore()` with `onUnreachable:"drop"`; the drop calls `session.logout(ws)` → `useSession` re-renders → `App.tsx` shows `LoginScreen`. Fix in the SDK (not the shell) so native gets the same guarantee; no core/gateway change (the gateway already 401s a dead token). Regression: `app/sdk/tests/restore-liveness.gateway.test.ts` (4 cases vs the REAL spawned gateway — live-kept, dead-401-dropped, unreachable-dropped, unreachable-kept) + a Playwright e2e (login→create→kill+reboot gateway→reload→asserts "Lazybones / Sign in", no stale room). Lesson: a rehydrated credential must be proven live before the UI trusts it — "stored token read back" ≠ "session valid," and a swallowed/absent 401 renders as a confusing empty state | resolved | [app/stale-preview-session-shows-empty.md](app/stale-preview-session-shows-empty.md) |
| 2026-07-04 | rules | interactive `channel.post` in the Playground only ever showed ONE message (each post replaced the last) and the message had no timestamp, sorting as the oldest. Root cause: `rules.run` derives its logical clock `now` from the request's `ts` field, defaulting to `0` when absent (`host/src/rules/mod.rs:140`); the messaging handles (`rules/src/verbs/channel.rs::post` + `inbox.rs`/`outbox.rs`) derive a DETERMINISTIC id from `now` (`rule-channel-{now}-{seq}`) for scheduled-re-run idempotency. But the interactive path never sends `ts` — the UI client doesn't, and the gateway route `role/gateway/src/routes/rules.rs::run_rule` (unlike `routes/datasources.rs`/`routes/dashboard.rs`, which thread `gw.now()`) never injected the live clock — so `now` collapsed to `0` every interactive run: same id (`rule-channel-0-{seq}`) → store upserts the same row; `ts:0` → sorts oldest. All three messaging handles share the same `now`, so all three carried the bug. Fixed by injecting the live wall clock at the gateway edge when the caller omits `ts` (`if input.get("ts").is_none() { input["ts"] = json!(gw.now()) }`) — clock injected at the edge, core stays clock-free; a caller that supplies its own `ts` keeps its idempotent write. Regression: `rules_routes_test.rs::interactive_channel_posts_append_with_distinct_ids_and_ascending_ts` (two runs over advancing fixed clocks → 2 distinct-id/ascending-ts rows; a same-clock re-run upserts, still 2). Lesson: a deterministic id derived from an injected clock is only safe when the clock advances — a forgotten edge injection turned an "idempotent" write into "overwrite-forever" data loss | resolved | [rules/interactive-rule-messaging-writes-collapse-to-now-0.md](rules/interactive-rule-messaging-writes-collapse-to-now-0.md) |
| 2026-07-03 | jobs | a long-running dev `node` pegged a constant 100% of one core even when idle; API responses felt progressively slower the longer it ran (user suspected the newly-added external-agent code). Ruled out the external-agent runtime (per-run only) and the demo flow's *firing* (disabling the flow didn't drop CPU) by reproducing on a store COPY on a spare port: a fresh/empty store idles at ~2%, a copy of the bloated dev store pegs 100% immediately — so the cost scales with table size, i.e. a scan. Root cause: `spawn_agent_reactors` (2 s tick, added 2026-07-01 "added in channel agent") and `spawn_flow_reactors` (5 s tick) both call `lb_jobs::pending`, which **walks the entire `job` table** page-by-page and filters kind/status in Rust — no indexed query. The demo `flipflop`/`cron` flow had minted ~2,900 terminal `job` rows (+ ~2,900 `flow_run`, ~6,300 `flow_step_output`) that nothing ever purges; at ~0.65 s per 500-row page in a debug build one `pending` pass exceeds the 2 s tick, so the reactors scan back-to-back with no idle, forever. The full-walk is itself the earlier fix for `pending-first-page-only-misses-jobs.md`, whose own comment flagged "the jobs table accumulates rows from every kind forever" — this is that hazard realized. Fix scoped in `scope/jobs/job-retention-scope.md`: (1) indexed status/kind `pending` query so the drain is O(pending) not O(table); (2) bounded retention (store `capped.rs` trim precedent) for terminal `job`/`flow_run`/`flow_step_output` rows. Immediate relief: wipe `.lazybones/data/dev-store` + `make kill && make dev`. Lessons: a recurring full-table scan on a timer is a latent O(table) cost bomb (fine on empty, fatal when aged); any table written on every routine event needs a retention policy from day one; trust the fresh-vs-copied-store repro over the suspected new code. **FIXED 2026-07-03** (`sessions/jobs/job-retention-session.md`): (1) `pending` is now an indexed `data.kind`/`data.status` query backed by `DEFINE INDEX job_kind_status` (`jobs/schema.rs`, lazy per-ns on first `create`) — O(pending); (2) bounded count-retention (default 500/ws) for terminal `job` (`jobs/retain.rs`) and finished `flow_run`+`flow_step_output` in tandem (`flows/retain_runs.rs`), swept on the flow reactor tick every 30th tick (`flows/retention_sweep.rs`); delete predicate is `status IN (terminal)` only — a resumable job/run is never trimmed. Regression tests: `jobs/tests/retain_test.rs` + `host/tests/flows_retention_test.rs`. `make purge-store` added for immediate dev relief | **resolved** | [jobs/node-pegs-cpu-reactor-rescans-job-table.md](jobs/node-pegs-cpu-reactor-rescans-job-table.md) |
| 2026-07-03 | frontend | after wiring frames-in, the standalone `ExtWidget.test.tsx` (5 v2-lifecycle tests) failed with `useDashboardWs: no DashboardCacheProvider in tree`: the new `useVizFrames` called the STRICT `useDashboardWs()` (which throws outside the provider to stop a dashboard read hook from keying under an unscoped `""` ws), but those tests mount `<ExtWidget/>` standalone. Cause: a v2 ext tile is a legitimately broader consumer than the built-in read hooks — it can mount outside a dashboard (reads through its own bridge, needs no frames), so requiring the cache provider to even MOUNT the widget was wrong. Fixed by adding `useDashboardWsOptional()` (returns `null` outside a provider, strict `useDashboardWs` unchanged) and having `useVizFrames` enable its `useQuery` only when `ws !== null` (`enabled = hasTarget && ws !== null`), passing a throwaway `STANDALONE_CLIENT` when there is no provider so the hook satisfies rules-of-hooks without a `QueryClientProvider`, and `undefined` when a provider exists so context resolves the SAME client (dedup with `useVizQuery` preserved). Without a ws: no fetch, empty frames — v2 tile unaffected, data tile only resolves under the cache that supplies the ws (no unscoped key, no cross-ws bleed). Regression: `pnpm test` 426 green + `framesIn.gateway.test.tsx` v2-compat + ws-isolation. Lesson: a strict "must be inside provider X" guard is right for a narrow consumer but a bug for a broader one that legitimately renders outside X — add an explicit optional variant, don't loosen the strict guard | resolved | [frontend/ext-widget-standalone-mount-throws-no-dashboard-cache-provider.md](frontend/ext-widget-standalone-mount-throws-no-dashboard-cache-provider.md) |
| 2026-07-03 | agent | `agent_routed_test::an_edge_invokes_the_hub_agent_over_the_routed_namespace` failed after the active-agent-wiring diff: the routed edge→hub invoke reached the hub agent but ran the honest `UnconfiguredModel` ("no in-house model is configured on this node") instead of the **mock model** the test installed via `RuntimeRegistry::with_default(mock)` → `serve_agent`. Root cause: Slice 2's per-run model override (`RunContext.model_override`) was set **unconditionally** for the default runtime in `dispatch.rs` — `Some(resolve_workspace_model(…))` — but `resolve_workspace_model` never returns `None` (with no pick + no node model it returns the `UnconfiguredModel` placeholder, whose fallback tier reads `node.runtimes().default_model()`), and `InHouseRuntime::run` *prefers* the override over its registered model. So a workspace with no pick forced `UnconfiguredModel`, shadowing the model the runtime was registered with. The trap: the override's node-fallback reads `node.runtimes()`, but the registry a run is dispatched *through* is the one passed into `invoke_via_runtime`, which is NOT always `node.runtimes()` — production `serve_agent` gets `node.runtimes()` (so they coincide and the scope's own `agent_active_model_test`, which uses `&node.runtimes()` + a real pick, stayed green), but the routed test legitimately boots a bare hub and passes a separate mock-carrying registry. Fixed in `dispatch.rs`: only override with a **configured** model — `resolved.is_configured().then_some(resolved)` — so an unconfigured resolution yields `None` and `InHouseRuntime` falls back to its registered model, preserving the ladder (active pick → registered runtime model → that model's own unconfigured answer). Rules unaffected (they call `resolve_workspace_model` directly and still want the placeholder for the honest "AI not configured for rules"). Regression: the routed test is the guard (fails-before at `:120` / passes-after); `agent_active_model_test::the_in_house_loop_drives_the_picked_workspace_model` pins the other direction. Lesson: a per-run override that "always wins" must only win when it carries a REAL value — a resolver that returns a placeholder instead of `None` silently shadows a lower tier; and a fallback reading global node state diverges from a run dispatched through a passed-in registry | resolved | [agent/model-override-shadows-registered-runtime-model.md](agent/model-override-shadows-registered-runtime-model.md) |
| 2026-07-03 | auth | Settings → Agent showed "No agent definitions available for this node" though boot logged `seeded 6 agent definitions` and a **fresh** curl to `agent.def.list` returned 6. The UI reads the catalog over the MCP bridge (`POST /mcp/call`), not the `/agent/defs` REST route the working curl hit — different transports. Ruled out server dispatch (fresh-token `/mcp/call` returned all 6 with the right `{definitions:[…]}` shape) and CORS (preflight + POST both carried `access-control-allow-origin: *`); a bogus-token `/mcp/call` returned **401**, and `useAgentCatalog`'s `Promise.allSettled(...) ? value : []` collapses ANY rejection (401 included) into `definitions = []`, which renders the identical empty-state text as a genuinely empty catalog. Root cause: `Gateway::boot()` minted the token-signing key with `SigningKey::generate()` **every process start** — a fresh random key — while the SurrealKV store is durable (`LB_STORE_PATH`). So across a restart of the persistent dev node the browser's rehydrated (key-A) `localStorage` token 401'd against the new (key-B) gateway, and the catalog silently emptied. Fixed (1) backend: new `role/gateway/src/signing_key.rs` persists the 32-byte seed at `<store>.signing-seed` beside a durable store and reloads it via `from_seed` (ephemeral `generate()` only when no store path) — tokens now survive a restart; (2) frontend: `http.ts` clears the session on an authenticated 401 (fall back to login) but not on a 403 (capability Denied). Regression: gateway unit tests (a pre-restart token verifies after restart; a different store gets a different key) + `http.session-expiry.test.ts`. Lessons: persist the signing key WITH the store, not the process; and a swallowed rejection must not render as "empty" — distinguish "the fetch failed" from "there is nothing" | resolved | [auth/signing-key-not-persisted-invalidates-sessions.md](auth/signing-key-not-persisted-invalidates-sessions.md) |
| 2026-07-03 | frontend | the dashboard panel-editor gateway test "full option surface" failed at `getByLabelText("tab Field")` — the editor's options rail migrated to the shared `@nube/panel` `NavMenu` (renders `aria-label={item.label}` = bare `"Field"`), but the test still clicked the old `PanelTabs`-style `"tab Field"` label, so the Field tab was unreachable and the whole fieldConfig-through-the-UI assertion was dead. Stale test, not a code regression (verified by reproducing against `HEAD`'s `PanelEditor.tsx`). Fixed the assertion to `getByLabelText("Field")`; no production change needed. Lesson: when a component migrates onto a shared primitive that changes accessible names, grep the suite for the old label scheme in the same change | resolved | [frontend/panel-editor-tab-label-stale-after-navmenu.md](frontend/panel-editor-tab-label-stale-after-navmenu.md) |
| 2026-07-03 | frontend | on the rules workbench a user ran `query("timescale", "select * from site")` (which SUCCEEDED — 3 seeded sites in the grid) but reported "I can't even save a rule": the toolbar showed only **Run**, no Save. Root cause was two UX defects (not the engine): (1) `RulesView` rendered Save/Rename only inside `if (r.selectedId)`, so a freshly-authored, not-yet-saved buffer (`selectedId === null`) had **no Save button in the DOM** exactly when needed — persisting required discovering the rail's separate name-first "New rule" form, with no "save what I'm looking at" path and no `⌘S`; (2) the result region was a bare `max-h-[45%]` scroll box with no header, so a successful run produced a grid but nothing that read as success, and "not run yet" was indistinguishable from "ran, returned nothing". Fixed by adding `useRules.saveCurrent(nameForNew?)` (open rule → update in place; fresh buffer → `{needsName:true}` so the caller reveals an inline name field) + a `hasRun` flag; `RulesView` now shows Save ALWAYS (toolbar + global `⌘S`/`Ctrl+S`), dirty-aware; a new `ResultBar` status header summarises the run (`3 rows · 4 ms` / Running… / Failed / Not run yet); `RunResult`/`GridTable` moved to token-correct states (destructive token not raw `red-*`, skeleton not spinner, sticky/zebra table, dim `NULL` literal). Validated by a clean production `vite build` + a token-faithful visual render (the UI gateway suite is pre-existingly red repo-wide: `Invalid Chai property: toBeInTheDocument` — jest-dom matchers not attaching under current vitest, unrelated). Lesson: a "save what I'm working on" action must never be gated on the artifact already being saved — that inverts the flow; keep the primary persist action always reachable and let an un-named draft prompt for a name inline. **Follow-up in the same session:** rendering empty cells as a visible `NULL` (part of the redesign) surfaced a PRE-EXISTING data bug — a federated `query("timescale", "select * from site")` showed "3 rows" + correct `id`/`name` headers but EVERY cell was NULL. Cause: two row shapes feed one renderer — platform (SurrealDB) rows are OBJECTS keyed by column name, but federation (datasource) rows are column-aligned ARRAYS (the sidecar re-projects Arrow objects to positional arrays: `rust/extensions/federation/src/query.rs`). `GridTable` did `row[columnName]` → `undefined` on an array row → NULL. No test caught it (the pg federation test asserts only `rows.len()`, never a cell value/key). Fixed with `GridTable.cellAt(row, column, index)` reading by INDEX for arrays / by KEY for objects, and widening the wire type to `rows: (Record<string,unknown> \| unknown[])[]`. Also added a `table \| json` result-view toggle (`JsonView`) per user ask. Second lesson: when two producers feed one renderer, pin the row SHAPE in a test, not just the count — `rows.len()==3` passes whether cells are populated or all-NULL; and rendering absent data as a visible `NULL` (not a blank) is what exposed a silently-shipping bug | resolved | [frontend/rules-save-unreachable-on-adhoc-buffer.md](frontend/rules-save-unreachable-on-adhoc-buffer.md) |
| 2026-07-03 | frontend | clicking a dashboard whose cell mounts thecrew's `ext:thecrew/scene` graphics-canvas widget rendered the scene fine but then **wedged navigation** — clicking another dashboard / sidebar item did nothing. A first guess (the tile *throwing* on WebGL disposal) was WRONG; a Playwright drive of the real dev shell captured the actual errors: `Attempted to synchronously unmount a root while React was already rendering` + `PAGEERROR: removeChild: not a child` inside `commitDeletionEffectsOnFiber`. Root cause: the dashboard-cell federation path (`ExtWidget`) mounts the tile IN-PROCESS; the tile owns its OWN React root (`createRoot(el)`), and `ExtWidget`'s effect-cleanup called that root's `unmount()` **synchronously**. But cleanup runs during the SHELL's own render/commit (a route change unmounts the cell), so a nested synchronous `root.unmount()` made React double-remove the same DOM subtree → `removeChild: not a child` thrown INSIDE React's commit (uncatchable by our `try/catch`) → the shell commit aborted and nav wedged. Fixed host-side by DEFERRING the tile unmount to a `queueMicrotask` (runs after the shell commit) and NOT hand-clearing the container the tile root owns; applied to BOTH in-process mount points — `ExtWidget` (dashboard cell, the reported bug) and `ExtHost` (extension page, latent identical bug). `WidgetView` still wraps `<ExtWidget>` in `ExtErrorBoundary` as a belt-and-braces render-throw wall (not what fixed this). Verified real-browser: after → URL changes to `#/t/acme/rules`, sidebar survives, only a clean `THREE Context Lost`; no pageerror. Regression: `ExtWidget.test.tsx` "defers the tile unmount out of the synchronous cleanup" (asserts unmount is NOT synchronous during React's commit + the deferred microtask still runs it). Lesson: never call a nested `root.unmount()` synchronously from a React effect-cleanup — it double-removes shared DOM inside the parent commit (uncatchable) and aborts the render; defer it. And a 5-min real-browser Playwright repro beats guessing at a browser-only symptom | resolved | [frontend/dashboard-ext-widget-teardown-wedges-nav.md](frontend/dashboard-ext-widget-teardown-wedges-nav.md) |
| 2026-07-03 | frontend | the Control Engine wiresheet rendered its nodes + all handles but **no edges** (wire connections) — the `random.out → dewpoint.rh` wire never drew; no console error. Root cause: `CeEditor` parks new edges in `pendingEdges` and promotes them to RF's live `edges` only once both endpoints' handles register in RF's internal store — but promotion was gated by a `useStore(readyKey)` selector (which didn't re-fire as `nodeLookup.internals.handleBounds` mutated in place) AND a **fixed 1500ms grace timer from mount** that dropped anything still unresolved as "malformed". On a cold federated-bundle load (the CE page is a lazily-imported remote — slow first paint) the grace fired BEFORE handles finished registering, so `readyKey` stayed `""`, `pendingEdges` was nulled, and RF got zero edges (a `console.log` in the selector "fixed" it → the tell it was a timing race). Fixed by reading handle bounds FRESH via `rf.getInternalNode(id).internals.handleBounds` (no stale subscription), driving promotion off `useNodesInitialized()` + an rAF poll, and starting the grace clock only AFTER `nodesInitialized` (before init, "unresolved" = "not measured yet", not malformed). Verified real-browser: 0→1 edge path, stable over 3 cold loads. Regression: `ui/e2e/ce-edges.spec.ts` (real browser + node; jsdom can't reproduce a measurement-timing race). Gotcha: the ext bundle does NOT hot-reload — rebuild ce-wiresheet lib + ext UI + copy to `extensions-ui/`. Lesson: a promotion gate that needs a subscription to re-fire AND a fixed timer from mount is a race on a slow load — key off the framework's own "initialized" signal, read state fresh, and never start a give-up timer before the awaited thing could have happened | resolved | [frontend/ce-edges-never-render-readykey-race.md](frontend/ce-edges-never-render-readykey-race.md) |
| 2026-07-03 | frontend | picking **"AI widget"** and saving (before generating anything) failed `cell (genui): view is "genui" but options.genui is missing` and the panel read `invalid genui widget (no IR)` — a genui widget couldn't be ADDED the normal way. Root cause: a freshly-picked genui cell has no IR yet (the author generates it in the "AI widget" tab after), but both the host `dashboard.save` branch (`ok_or_else(… "missing")?`) and `GenUiView.cellIr` (returned `null`) treated "no IR" as MALFORMED, not as an un-authored draft — so the blank cell was rejected at save and shown as invalid. Wrong model: adding a blank widget you configure later is normal (like a blank timeseries). Fixed by distinguishing a DRAFT from a malformed spec at both boundaries: the host returns `Ok` for a missing `genui` block / a block with no `ir` (only a present, NON-object `ir` is `BadInput`), and `GenUiView` returns `"draft" | "invalid" | {ir}` — a draft renders a muted "author me" placeholder, a present-but-broken IR renders "invalid genui widget (malformed IR)". Regression: host `allows_an_unauthored_draft`, `GenUiView.test.tsx` draft/invalid, gateway `SAVES an un-authored draft`. Lesson: a "configure it later" widget must be creatable EMPTY — validation that assumes a fully-authored artifact blocks the add flow; distinguish un-authored draft (allowed, guided) from malformed (rejected) | resolved | [genui/adding-genui-widget-rejected-no-ir.md](genui/adding-genui-widget-rejected-no-ir.md) |
| 2026-07-03 | frontend | after the tree decode + bridge `watch` fixes, the control-engine canvas rendered the graph but showed **no live values** and read **disconnected**; the live `control-engine.watch {appliance:"aaaa"}` → `/series/{s}/stream` opened but streamed **0 bytes**. Root cause: `watch` with no explicit `scope` built an **empty `CovScope`**, so the crate sent `{"type":"subscribe"}` with no `components` — and the ce-studio engine only pushes COV frames for **explicitly-subscribed** components (raw WS probe: empty subscribe → 0 value frames; `[100008..100013]` → 36 frames/4s). The UI's default watch ("whole appliance", no scope) IS the empty case. Fixed extension-side: `expand_scope` (`watch/verb.rs`) materializes an empty scope into the appliance's full component-UID set by walking the tolerant raw tree (`watch/scope_uids.rs`, skips synthetic root `uid 0`) and injecting `scope.components` before arming; an explicit scope is honoured verbatim (never widened); an unreachable engine falls back to the given scope (a gap, not a failed call). The populated scope keys a different series (`args_hash` folds scope) — that's the point: the empty-scope series carried nothing. Live-verified: series `5ff2…`→`3a37…`, stream 0→3164 bytes with real `{"kind":"cov","values":[…]}` frames. Regression: `watch_scope_expand_test.rs` drives the real `expand_scope`→`raw_tree`→`collect` path over a REAL HTTP round-trip serving a captured-real `/nodes` envelope (rule 9). Lesson: "empty means everything" is a trap when the downstream reads empty as "nothing" — materialize the default into the concrete UID set at the arming boundary | resolved | [frontend/ce-canvas-empty-cov-scope-no-live-values.md](frontend/ce-canvas-empty-cov-scope-no-live-values.md) |
| 2026-07-03 | frontend | rendering a saved `view:"genui"` cell warned `Cannot update a component (GenUiView) while rendering a different component (TargetProbe)`. Root cause: `GenUiView` resolves one data target per `sources[]` entry via a `TargetProbe` child that calls `usePanelData` and reported its resolved `RefData` up to the parent's state through `useMemo(() => onData(...))` — but `useMemo`'s factory runs DURING render, so a child's render synchronously `setState`'d the parent (the classic setState-in-render trap). Widget rendered fine (warning non-fatal) but it's a real React contract violation. Fixed by reporting from `useEffect` (post-commit) keyed on the data content-signature; the `onData` setter already short-circuits unchanged values so there's no loop. Regression: the genui gateway test spies `console.error` and asserts the warning never fires on the real-data render. Lesson: a child reporting derived data up to a parent must do it in `useEffect`, never `useMemo`/render — a per-item probe component is the right shape for a one-per-item hook, but it must report from an effect | resolved | [genui/genui-probe-setstate-in-render.md](genui/genui-probe-setstate-in-render.md) |
| 2026-07-03 | frontend | after the tree decode fix (graph + wires render), the control-engine canvas still showed **no live values** and the `ConnectionStatus` badge read **disconnected**. Root cause: live COV values ride a stream (`bridge.watch('series.watch')` → the shipped `/series/{s}/stream` SSE), and the CE transport's `openStream` degrades to a static canvas when `typeof bridge.watch !== "function"`. The extension contract declares `watch?` OPTIONAL and the vendored transport degrades gracefully — but the shell's own `makeBridge` (`ui/src/features/ext-host/bridge.ts`) only ever implemented `call`, never `watch`, so for EVERY federated page the live half was dead (manifest scope was correct; the engine WS was fine). Fixed by implementing `watch` in `makeBridge` mapping `series.watch` onto the existing `openSeriesStream` SSE client, scope-gated like `call` (non-series verb / empty series → no-op unsubscribe; no gateway → static, unchanged). Regression: `ExtHost.gateway.test.tsx` asserts the scope-gate + series-guard no-ops. Lesson: an OPTIONAL contract capability silently no-ops when the host forgets to provide it — the page "works" (structure loads) while the live feed is quietly off; when a contract says `watch?`, the shell owning the seam must actually wire it | resolved | [frontend/ce-canvas-disconnected-no-live-values-bridge-missing-watch.md](frontend/ce-canvas-disconnected-no-live-values-bridge-missing-watch.md) |
| 2026-07-03 | extensions | container devkit build dies `linker /home/user/.local/bin/zigcc not found` — host `.cargo/config.toml` zig linker leaks into the pinned image | resolved | [extensions/devkit-container-build-inherits-host-zig-linker.md](extensions/devkit-container-build-inherits-host-zig-linker.md) |
| 2026-07-03 | frontend | the control-engine canvas rendered **blank** under a red toast: `control-engine codec error: /nodes?depth=1&withEdges=true: decoding response: missing field \`source_uid\` at line 1 column 7041`. Root cause: the pinned external CE client crate `rubix-ce` (rev `51ab97e`) declares `EdgeDto.source_uid`/`target_uid` as **required** (`types.rs:268`, no `#[serde(default)]`), but the real appliance emits a **dangling edge** that omits `source_uid`; serde fails the WHOLE `Tree` decode inside `get_tree` → "bad host response" → editor gets no graph. The strict typed hop was pure downside — the wiresheet already consumes the `{nodes,edges}` JSON verbatim. Fixed extension-side (the crate is an uneditable git dep): a **tolerant raw fetch** (`tools::raw_tree`) does its own `GET /api/v0/nodes?…&withEdges=true`, unwraps the `{data:{nodes,edges}}` envelope as untyped `serde_json::Value`, and passes it straight through so a `source_uid`-less edge survives (the wiresheet renders the dangling edge, not the transport). `serve.rs` routes real `control-engine.tree` here (`!is_fake()`); the `ce_fake` stub keeps the typed `get_tree`. Rule-9 regression: a real-envelope fixture with a `source_uid`-less edge asserts pass-through (23 lib tests green). Lesson: a strict typed transport hop that only re-serializes to JSON can ONLY reject a shape the consumer would tolerate — when the consumer speaks raw JSON, fetch raw JSON | resolved | [frontend/ce-tree-missing-source-uid-blanks-canvas.md](frontend/ce-tree-missing-source-uid-blanks-canvas.md) |
| 2026-07-03 | auth | a `store:skill/*:write` grant can't reach a **core** skill (`grant_skill("core.lb-cli")` → `Denied`) while it works for a flat user id. Root cause: the caps grammar splits a resource on **`/` AND `.`**, so `skill/core.lb-cli` is THREE segments (`skill`,`core`,`lb-cli`) but `skill/*` is two — a single `*` matches one segment and leaves `lb-cli` unmatched. The shipped dev-login granted `store:skill/*`, so a real admin could neither grant nor load any core skill (the served core path was silently broken). Fixed by granting the skill surface with the recursive-tail `**` (`store:skill/**:read|write`) — covers a dotted core id AND a flat user id; no grammar change (the `*`-never-crosses-a-segment invariant is correct). Lesson: an id that embeds a `.`/`/` is multi-segment — grant the tail (`**`), not one segment (`*`) | resolved | [auth/skill-star-cap-misses-dotted-core-id.md](auth/skill-star-cap-misses-dotted-core-id.md) |
| 2026-07-02 | extensions | a native sidecar's restart budget exhausts (`restart budget exhausted after 5 restarts`) and never recovers — after a transient crash-loop every call fails permanently until the whole node is bounced. Root cause: the `restarts` counter is monotonic + terminal (no decay, no re-arm), and `restart_native` reuses the same budget-checked path so it can't rescue an exhausted sidecar. Fixed at two layers: a new budget-**ignoring** `Sidecar::rearm` behind a capability-gated `native.reset` verb (POST /extensions/{ext}/reset + a Reset button in the Extensions console), and auto-decay — a successful call after the `Backoff.cooloff` window (default 30s) zeroes the count, so a transient crash self-heals. Decay hangs on the call path (not a new health-poll loop) to match the tier's on-demand model. Lesson: a bounded budget with no decay + no reset turns a transient fault into a permanent outage — cap crash loops, but always give exhaustion a recovery path | resolved | [extensions/sidecar-restart-budget-exhausted-permanent.md](extensions/sidecar-restart-budget-exhausted-permanent.md) |
| 2026-07-02 | frontend | **adding a node** on the control-engine canvas threw `Cannot convert undefined or null to object` and the add appeared to do nothing. Root cause: the engine's add response (POST `/nodes`) comes back leaner than a GET-loaded component — it **omits `properties`** on a brand-new node — but the `Component` type declares `properties` NON-optional, so the structural store's bare `Object.values(c.properties)` in `indexComponentProperties` (`store.ts:44`) crashed. Reproduced only on add (GET always hydrates `properties`); the store was the odd one out (every other `properties` consumer already used `?? {}`). Fixed at the boundary — a `normalizeComponent` in `rest.ts` restores `properties ?? {}` once for every bare-`Component` endpoint (`addNode`/`updateNode`/`patchOverrides`/`groupComponents`) so the client sees the GET shape — plus store hardening (`Object.values(c.properties ?? {})`) as defense-in-depth. Regression in `store.test.ts` fails-before with the exact reported error. Lesson: a mutating endpoint's response can be leaner than the read endpoint's — normalize to the read shape ONCE at the fetch boundary so the type's invariant holds, don't sprinkle `?? {}` per consumer (and miss one) | resolved | [frontend/add-node-crashes-object-values-undefined-properties.md](frontend/add-node-crashes-object-values-undefined-properties.md) |
| 2026-07-03 | frontend | the control-engine federated page **took over / re-themed the whole shell** once it mounted. Root cause is CSS leaking from injected stylesheets into the host document (a federated page renders in-process). FOUR global-write classes, fixed in two rounds. **slice-9 (07-02):** TWO stylesheets shipped Tailwind Preflight (the GLOBAL `*`/`html`/`body` reset) — `tokens.css` (`@tailwind base`) + vendored `ce-wiresheet.css` (`@import 'tailwindcss'`, also ~200 unscoped `.flex`/… utilities); the reset re-reset the LIVE shell and the sidebar's flex layout collapsed. Fixed: drop Preflight, scope utilities under `.ce-page`/`.ce-wiresheet`, `tokens.css` inherits host `:root` via a `.ce-page` fallback. **slice-9.1 (07-03, "no Preflight ≠ no leakage"):** a full global-write sweep found THREE more — (1) `:root`/`@theme :root,:host` token writes (`--card`/`--border`/`--radius-md`/… same names as the host) that overrode 8+ shell tokens document-wide (shadcn cards flipped dark in light mode); (2) ~150 bare `.react-flow*` rules from `@xyflow/react` colliding with the host's system/data/flows canvases; (3) a JS-injected `<style>` (`EDGE_SELECTED_CSS`) with an unscoped `.react-flow__edge` — invisible to a CSS-file audit. Fixed upstream (S2): `wiresheet-theme.css` tokens `.ce-wiresheet`-scoped + host-inheriting (`var(--card, …)`); a build-time `scope-css.ts` Vite plugin rewrites `:root`/`:host`→`.ce-wiresheet` and prefixes every `.react-flow` (incl. `@media`-nested) in the FINAL CSS; the JS `<style>` selector scoped at source. Guard hardened: `global-scope-audit.test.ts` (zero `:root` writes + zero unscoped `.react-flow` CSS rules in built CSS AND JS chunk) + `scope-css.test.ts` (transform unit). Green: ext-ui 35, ce-wiresheet 153. `proof-panel` has the identical `@tailwind base`+`:root` leak — follow-up. Lesson: "no Preflight" is NOT "no leakage" — an injected/library stylesheet must ship NOTHING global (reset, utilities, `:root` tokens, vendor rules, JS-injected `<style>`); sweep for every global write, not just the reset | resolved | [frontend/ce-page-css-preflight-leaks-into-shell.md](frontend/ce-page-css-preflight-leaks-into-shell.md) |
| 2026-07-02 | frontend | opening the control-engine extension page **blanked the whole shell** (nav + sidebar vanished) with `Cannot read properties of undefined (reading 'bridge') at openStream`, then React "synchronously unmount a root" / "removeChild: not a child" errors. Two layers: (1) `CeEditor` called the injected transport's `openStream` through a **detached local** (`const f = transport.openStream; f(...)`), so the real `BridgeTransport.openStream` (a prototype method reading `this.bridge`) got `this===undefined` and threw on mount — invisible because the in-repo `MockTransport` **arrow-binds** its methods (the test was written around the bug); (2) a federated ext renders **in-process against the shell's React**, so that render throw unwound the SHELL tree, and `ExtHost`'s synchronous cleanup then fought React's commit. Fixed by calling `openStream` as a method (cast the transport, not the fn), a new `ExtErrorBoundary` crash-wall around `ExtHost`, guarded `ExtHost` teardown, and a stream-effect cleanup that closes + nulls the module-level `streamRef` (a remount was skipping arm → static canvas + leaked sub). Regression: a `ThisReadingTransport` (prototype methods reading `this`) proves the editor doesn't detach — fail-before threw `reading 'marker'`, the exact prod shape. Lesson: invoke a seam's methods AS methods (a class transport reads `this`); an arrow-binding test-double certifies a contract the real code violates; a federated ext needs a crash wall | resolved | [frontend/ce-page-crashes-openstream-detached-this-drops-shell.md](frontend/ce-page-crashes-openstream-detached-this-drops-shell.md) |
| 2026-07-02 | flows | disabling/deleting a source flow left the ext's live socket open — `disarm_source` ran, cleared the `armed` marker, returned `Ok`, but **never called `<ext>.disarm`**. Root cause: it resolves the ext tool from the marker's `_type`, but `arm_source` wrote `{armed,series}` with **no `_type`**, so the read-back was empty and the disarm call was skipped (invisible — disarm still cleared the flag). Can't read the type from the flow at disarm time either: an orphan disarm (flow deleted) has no flow. Fixed by persisting `_type` on the armed marker (`arm_source` + `reconcile_flows` stamp `node_type`); the new orphan sweep relies on it. Lesson: a teardown that resolves its target from a durable marker needs the arm path to write everything teardown needs onto it — assert the tool-call, not just the cleared flag | resolved | [flows/disarm-cant-resolve-ext-tool-arm-omitted-type.md](flows/disarm-cant-resolve-ext-tool-arm-omitted-type.md) |
| 2026-07-02 | auth | an installed extension's federated page 403'd on load (`out_of_scope`, then `denied`) — the ext's user-facing tool caps only reached a user if HARDCODED in the dev login (`member_caps`), not via the shipped grant store. Fixed generically: install grants each `[ui]`/`[[widget]]` scope tool (∩ granted) to `role:workspace-admin` (new `host/authz/grant_ui.rs`); `resolve_subject_caps` folds a role subject's DIRECT grants (not just its immutable `role_define` record); login merges `resolve_caps` into the token. Installing ANY extension now makes its page reachable by admins with zero login edits — durable + revocable. Lesson: an extension's caps belong in the grant store, never a login hardcode | resolved | [auth/ext-page-caps-require-grant-not-login-hardcode.md](auth/ext-page-caps-require-grant-not-login-hardcode.md) |
| 2026-07-02 | extensions | a native sidecar's host callbacks 401'd (`invalid or expired credential`) with correct caps — its `LB_EXT_TOKEN` was minted by `install_native` with the node's THROWAWAY boot key because native roles were mounted BEFORE `Gateway::new_live` installed the shared signing key; the gateway then verified callbacks with its own key → mismatch (federation's boot seed hit this silently). Fixed by mounting native roles AFTER the gateway is built so all share one signing identity. Lesson: a sidecar callback token must be minted with the key the gateway verifies with — boot ordering is load-bearing | resolved | [extensions/sidecar-callback-401-key-minted-before-gateway.md](extensions/sidecar-callback-401-key-minted-before-gateway.md) |
| 2026-07-02 | extensions | Studio `devkit.build` of `control-engine` dies with `exit status: 101` fetching a private git dep — inherited `GIT_ASKPASS` from the VS Code-launched node process | resolved | [extensions/devkit-build-fails-exit-101-private-git-dep.md](extensions/devkit-build-fails-exit-101-private-git-dep.md) |
| 2026-07-02 | extensions | real-engine `control-engine.tree` fails "LB_GATEWAY_URL is not set" after S4 routed graph verbs through the registry callback | resolved | [extensions/ce-tree-fails-without-gateway-real-engine-tier.md](extensions/ce-tree-fails-without-gateway-real-engine-tier.md) |
| 2026-07-02 | extensions | a sidecar's `store.query` callback returns `Denied`, masking a SurrealQL parse error (gateway `/mcp/call` collapses every error to 403) | resolved | [extensions/mcp-call-collapses-all-errors-to-denied.md](extensions/mcp-call-collapses-all-errors-to-denied.md) |
|---|---|---|---|---|
| 2026-07-02 | external-agent | in a channel, the `/agent` command let the user type a goal but offered **no control to pick a runtime** (e.g. `open-interpreter-default`) — so a plain run hit the in-house `default`. The node was innocent: with `EXTAGENT=1` boot printed all four runtimes, `agent.runtimes` over the real gateway returned them, and `tools.catalog`'s `agent.invoke` carried the `x-lb:{widget:"runtime"}` arg — the handover's feature-not-registering and serve-wiring hypotheses were both DISPROVED (the gateway shares the same `Node` via `node.clone()`). Root cause was UI: `CommandPalette.tsx`'s arg rail (`activeArg`) only ever targeted **required, unfilled** args; `runtime` is **optional**, so after the required `goal` was committed the rail went idle and the `RuntimeArg` dropdown — which only renders as the single active arg — never mounted. Fixed by adding a third `activeArg` tier: the first **shown, optional, INLINE** arg, targeted whether or not it holds a value so an in-place widget stays mounted+editable after preselecting its default. Optional CHIP (`text`) filters are unaffected. Reproduced as a failing real-gateway test (`CommandPalette.agent.gateway.test.tsx`); regression added in `CommandPalette.dispatch.test.tsx`. Lesson: an optional INLINE widget needs its own rail tier — a required-only walk makes it structurally unreachable | resolved | [external-agent/no-runtime-picker-option-in-channel.md](external-agent/no-runtime-picker-option-in-channel.md) |
| 2026-07-02 | frontend | the `ext:thecrew/scene` dashboard cell mounted in-process with a real-sized `<canvas>` and the scene doc loaded over the bridge (`scene-widget`, not the empty state), yet the cell rendered **visually blank** — while the same scene rendered fine on the full-page `[ui]` view. Two layers: (1) the cell used the editor page's FIXED ortho framing (±350 world units at `zoom 1.6`), showing only a tiny center crop in a small grid cell → the offset AHU-1 shapes fell outside the frustum; a cell needs **fit-to-cell** framing (scene bounds → fit zoom → pan to center). (2) A first-pass `useEffect` fit STILL rendered blank because drei's `<OrthographicCamera makeDefault zoom/position>` **re-applies its declarative props every re-render**, clobbering the effect-set pose. Fixed with a pure `canvas/fit-bounds.ts` + `canvas/FitCamera.tsx` driving the ortho camera **every frame** via `useFrame`, gated behind a `fit` prop the read-only widget passes (`CameraRig` drops MapControls in fit mode; the editor page is untouched). Verified live in a seeded AND a palette-built cell. Lesson: a cell needs fit-to-cell framing, not the page's fixed zoom; own a drei `makeDefault` camera's pose in `useFrame`, never a one-shot effect | resolved | [frontend/scene-widget-cell-renders-blank-fixed-camera-fit.md](frontend/scene-widget-cell-renders-blank-fixed-camera-fit.md) |
| 2026-07-02 | extensions | publishing `thecrew` live, its Graphics page threw `Could not load thecrew: process is not defined` on load — the nav slot appeared + `remoteEntry.js` served 200, but the remote's module eval crashed before `mount`. Root cause: three.js / `@react-three/fiber` (BUNDLED in this remote — the federation payoff) read `process.env.NODE_ENV` at eval, and a Vite **lib** build (unlike an app build) does NOT inject it, so bare `process.env.NODE_ENV` (9 occurrences) survived into the browser bundle. Invisible to unit/gateway suites (Node/jsdom HAS `process`); only the live shell revealed it. Fixed with `define: { "process.env.NODE_ENV": JSON.stringify("production") }` in the remote's `vite.config.ts` (→ 0 leaks). Lesson: a federated remote that bundles a lib reading `process.env.*` must define it in its OWN lib build | resolved | [extensions/federated-lib-build-leaks-process-env-node-env.md](extensions/federated-lib-build-leaks-process-env-node-env.md) |
| 2026-07-02 | extensions | after the `process` fix, the `thecrew` Graphics page still failed live with `remote does not export a \`mount\` function`. Root cause: the shell's `pickMount` resolves the PAGE mount by the name **`mount`** (the frozen federation contract, as proof-panel exports), but thecrew's `remoteEntry.ts` exported the page as `mountPage`. Invisible to the unit suite because `mount.test.tsx` imports `mountPage` directly and never goes through `pickMount`. Fixed by exporting `mount` (page) + keeping `mountPage` as an alias for the unit test + `default = { mount, mountWidget }`. Lesson: the federation page export MUST be `mount`; a unit test that imports the export by name can't catch a shell-contract mismatch — the live e2e is the guard | resolved | [extensions/federated-page-export-must-be-named-mount.md](extensions/federated-page-export-must-be-named-mount.md) |
| 2026-07-02 | frontend | building the `thecrew` extension UI (`build.sh`, proof-panel pattern) installed **no deps** — a plain `pnpm install` inside `rust/extensions/thecrew/ui/` reports "Scope: all 3 workspace projects" and never installs THIS package, so `node_modules/.bin/vite` is absent and `vite build` fails (`tsc: not found` on typecheck). Root cause: the extension `ui/` has its own `package.json`+lockfile and lives OUTSIDE the repo-root pnpm workspace (globs `ui`+`packages/*`), but pnpm's workspace discovery walks UP and captures the nested package into the ancestor workspace by default. Fixed by installing with `--ignore-workspace` in `build.sh` (treats cwd as its own root) + a `.gitignore`. Guardrail is the flag + comment; proof is `./build.sh` emitting `ui/dist/remoteEntry.js`. Follow-up: proof-panel/ui has the same latent trap. Lesson: an extension UI intentionally outside the repo workspace MUST install `--ignore-workspace` | resolved | [frontend/thecrew-standalone-pnpm-install-walks-to-repo-workspace.md](frontend/thecrew-standalone-pnpm-install-walks-to-repo-workspace.md) |
| 2026-07-02 | frontend | wiring `@nube/panel` into `ui` made the app's **left sidebar vanish** and the editor's selected nav item render as a **fixed near-black block** in the host's light theme. Root cause: the shared UI packages' stylesheets were built like app stylesheets — (1) a top-level `@import 'tailwindcss/utilities.css'` emitted ~200 **global** `.flex`/`.grid`/`.border`/… rules that collided with the host's own utilities (worse cross-minor: `ui` v4.1 vs panel's v4.3 `@property` internals), so the library's copy (imported after `globals.css`) won the cascade and broke the sidebar layout; (2) nav-rail's `--nr-*` were a **fixed dark palette**, so its selected item ignored the host light theme. Neither shows in jsdom tests — only the running app revealed it. Fixed by (a) scoping every generated utility under the package root via v4 nesting (`@layer utilities { .lb-panel { @tailwind utilities } }`, same for `.nav-rail`) so classes emit as `.lb-panel .flex` and can't leak, and (b) aliasing the tokens onto the host shadcn vars (`--nr-bg: var(--muted-bg, …)`, `--lbp-panel: var(--card, …)`, …) with dark fallbacks. Green after: nav-rail 12/12, panel 7/7, ui unit 322/322, editor gateway suites, `vite build`. Lesson: a Tailwind LIBRARY stylesheet must ship scoped-utilities + host-shadcn-aliased tokens + no preflight — never a global `@import 'tailwindcss'` and never a fixed palette | resolved | [frontend/library-css-leaks-global-utilities.md](frontend/library-css-leaks-global-utilities.md) |
| 2026-07-02 | frontend | `pnpm test:gateway` began failing before any test ran with `Cannot find module 'vite'/'vitest'` — looked like a test regression. Root cause (tooling, not product): a `git worktree` created to diff against the pre-change commit had its `node_modules` symlinked into `ui/node_modules`; pnpm hardened `ui`'s top-level dep links to absolute paths **through** the worktree, and `git worktree remove --force` left 24 dangling links (vite/vitest/react/tailwindcss/…). `pnpm install --force` reported "up to date" and did NOT repair them (lockfile matched, `.pnpm` store intact). Hand-repair was a trap — the worktree's commit had a different lockfile (e.g. `tailwindcss@3.4.19` vs the app's `@4.3.2`). Fixed with `rm -rf ui/node_modules && pnpm install`. Lesson: never symlink one checkout's `node_modules` into another; give a worktree its own install. When `test:gateway` can't find `vite`/`vitest`, it's a broken `node_modules`, not a test regression | resolved | [frontend/gateway-node-modules-symlink-corruption.md](frontend/gateway-node-modules-symlink-corruption.md) |
| 2026-07-02 | frontend | embedding the template interpolator into the iframe `srcdoc` builder broke `tsc` with a bogus-looking `TS2349 "Type 'String' has no call signatures"` at the template-literal start. Root cause: a `//` comment inside the embedded `<script>` used **literal backticks** as code-quotes, which terminated the outer `` return `…` `` template literal early — the following `.toString()` parsed as real code. A `//` comment does NOT protect backticks inside a template literal. Fix: keep embedded-script comments backtick-free; treat `` ` ``, `${`, and `</script>` as the three sequences that must never appear raw in a `srcdoc` template literal. Guard: `tsc --noEmit` fails the whole file, so the typecheck step is the regression guard | resolved | [frontend/srcdoc-template-literal-backtick-break.md](frontend/srcdoc-template-literal-backtick-break.md) |
| 2026-07-02 | frontend | adding the new `@nube/nav-rail` workspace package (`workspace:*` dep of `ui`) made `ui`'s `tsc --noEmit` flood with `'Check'/'GitPullRequest'/… cannot be used as a JSX component` across every `lucide-react` importer — none of them touched. Root cause: the package declared **React 19** dev/deps (`react`/`@types/react`/`@types/react-dom@^19`, `lucide-react@0.453`) while `ui` is on **React 18**; promoting the pnpm workspace to the repo root hoisted a second `@types/react@19` type world, and lucide's `ForwardRefExoticComponent` resolved against v19's `ReactNode` (`bigint`-inclusive), unassignable to v18's — the app code was fine, the *type graph* was split. Fixed by aligning the package's dev React/types/lucide to `ui`'s (`react@^18.3.1`, `@types/react@^18.3.12`, `lucide-react@^0.460.0`) so the workspace is one type world; `ui` returns to its pre-change baseline (2 unrelated pre-existing errors), zero new. Lesson: `@types/react` is a per-app singleton — pin a new UI package's React types to the app it ships beside; a permissive peer range isn't enough when it also declares its own dev React | resolved | [frontend/react-types-19-collision.md](frontend/react-types-19-collision.md) |
| 2026-07-01 | channels | `/remind` couldn't create a reminder from the palette — submit failed at the host with `missing string arg: channel` (or `tool`/`target`) and the per-action-kind fields **never appeared** in the form. Root cause: the action fields are declared **optional** but are genuinely required *conditionally* (`channel` required **when** `action_kind=channel-post`, etc.), which plain JSON-Schema `required` can't express and the linear arg rail had no model for — so after the optional-arg fix they never surfaced. Fixed with a generic `x-lb.showIf` + `requiredWhenShown` form hint (request-side twin of `x-lb-render`): the descriptor declares each field's condition, `isShown`/`isActiveRequired` let the rail walk `required ∪ shown-and-required`, `buildArgs` collects only shown fields (no stale-kind leak), and `CronArg` seeds its default on mount (the builder only emits on edit, so `schedule` stuck empty). `/remind` now completes end to end (real-gateway create-form test). Follow-up (named): optional shown fields + a real schema-form renderer. Regression: descriptor conditional-hint test, palette conditional-required unit test, create-form gateway test, cron mount-seed test | resolved | [channels/palette-conditional-required-fields-unreachable.md](channels/palette-conditional-required-fields-unreachable.md) |
| 2026-07-01 | channels | picking `/reminders` (or any command with only **optional** args) in the palette didn't run it — the arg rail showed an empty `status…` text box and blocked send. Root cause: the palette treated **every** arg as fillable (`activeArg` picked any unfilled arg; `submitDisabled` keyed off it), so an optional `status` became active and blocked submit — a generic bug, surfaced now because `reminder.list` is the first command with only optional args. Fixed by respecting the schema's `required`: new `isRequired` helper, `activeArg` auto-targets only **required** unfilled args, so a only-optional-args command has no active arg → no text box, send enabled immediately. Optional args aren't reachable from the rail yet (a "add filter" affordance is a named follow-up). Regression: a only-optional command runs immediately with no arg box | resolved | [channels/palette-optional-arg-blocks-submit.md](channels/palette-optional-arg-blocks-submit.md) |
| 2026-07-01 | reminders | the channel `/reminders` table rendered **ONE JSON-blob row**, not N reminders — so a per-row control's `${id}` bound nothing (the interactive list was inert). Root cause: `viz.query`'s row-unwrap (`viz/frame.rs::ROW_KEYS` + its client mirror `useSource.ts::toRows`) unwraps rows from a fixed set of plural keys (`samples`/`items`/`rows`/`templates`/`dashboards`), and `reminder.list` returns `{reminders:[…]}` — `reminders` wasn't listed, so it fell through to "single object" → one row. Surfaced only by the real-gateway DOM render (Rust reminder tests read `{reminders}` directly, never through `viz.query`; UI unit asserts the envelope, not the mounted table). Fixed by adding `"reminders"` to both mirrored `ROW_KEYS` lists (additive, consistent with the other per-verb plurals); regression `reminders_shape_unwraps_to_n_rows`. Lesson: a list verb that wraps rows under its own plural must be in the `viz.query` unwrap set (both mirrors) or a table `source`-d at it renders one blob row | resolved | [reminders/reminder-list-not-unwrapped-to-table-rows.md](reminders/reminder-list-not-unwrapped-to-table-rows.md) |
| 2026-07-01 | reminders | **[open, pre-existing]** every `reminder.fire` (and the scheduled reactor firing) **denies** for a **dev-login / token-only** principal, even though its token provably carries the fired action's cap — so the channel `/reminders` "Run now" control is inert over the real gateway. Root cause: `fire_reminder` re-resolves the creator's caps **from the durable grant store** at fire time (deliberate, so a revoke applies) via `resolve_caps` → `grant_list`; but `dev_claims` synthesizes `member_caps()` into the **JWT only**, writing nothing durable → the re-resolve returns an empty set → the action's own gate denies. Pre-existing in the shipped reminder system (commit `b78a0bd`), surfaced by driving a real dev-login fire over HTTP. Unaffected: create/list/update(pause)/delete (they run under the token principal, no re-resolve). Deferred to a dedicated slice (security-semantics-sensitive — must not weaken revoke-takes-effect); likely fix is to persist member caps durably on login so the token's source-of-truth matches. Lesson: a fire-time re-resolve that reads the durable store assumes caps LIVE there — a token-only principal breaks that assumption | open | [reminders/reminder-fire-reresolve-misses-token-caps.md](reminders/reminder-fire-reresolve-misses-token-caps.md) |
| 2026-07-01 | reminders | the channel `/reminders` interactive table rendered but every row control (pause switch / Run now / Delete) was **inert** — the host rejected each write with `bad input: missing u64 arg: ts` before any effect. Root cause: the 100%-backend-driven row controls declare a `ts`-free `argsTemplate` (correct — the frontend is generic), the gateway `mcp_call` route forwards args verbatim (unlike flows/workflow which inject `"ts": gw.now()`), and `reminder.update/delete/fire` hard-required `ts` via `u64_arg` — **even though `reminder.create` already tolerated a missing `ts`**. Fixed by defaulting `ts` to the host clock (`opt_u64(...).unwrap_or_else(now_ts)`) in the three write verbs, matching `create`; kept `ts` out of the transport. Only the real-gateway UI loop caught it (Rust tests always passed `ts`; UI unit stubs the bridge). Lesson: the verb that owns the record defaults its logical `now`, not the caller or the transport — and the whole verb family must agree | resolved | [reminders/reminder-write-verbs-require-ts.md](reminders/reminder-write-verbs-require-ts.md) |
| 2026-07-01 | store | `pnpm test:gateway` flaked `not a member of any workspace` from `signInReal` (varies run-to-run) — many fresh `nextWs()` workspaces log into the ONE spawned gateway concurrently. Root cause (store): `Store` shares ONE embedded SurrealDB connection with a single mutable session; every verb did `use_ns(ws)` then its query as **two separate awaits** with no lock between, so on the multi-thread runtime two ops for different workspaces interleaved (`use_ns(A)` … `use_ns(B)` … A's query runs against B) — `membership_login_resolve` wrote the bootstrap membership into one namespace and read it back from another. The store's own concurrency tests never caught it (all same-workspace). Fixed by making `use_ws` hold a per-`Store` session mutex across the query (returns a `WsGuard` that derefs to `&Surreal<Db>`), so every `use_ns → query` pair is one critical section — closes the whole class, not just login. Lesson: a shared-session `use_ns` is a **global** mutation, not per-op scoping; the select + query must be one locked critical section | resolved | [store/concurrent-use-ns-namespace-race.md](store/concurrent-use-ns-namespace-race.md) |
| 2026-07-01 | flows | the data/JSON node pack's `switch` routed a payload to **both** the matched branch and the `else` branch — `switch_fires_only_the_matched_port` failed (`lo` ran `ok` when it should be `skipped`). Root cause: `matched_targets` evaluated every rule independently and unioned their targets, but the shared `ops::predicate::eval` `else` op **always returns true** (it is the fallthrough sentinel), so it matched on every payload additively. Fixed by making `else` a distinct fallthrough phase that fires only when **no** concrete rule matched (`stop_on_first` short-circuits the concrete pass). Lesson: a match-everything sentinel (`else`/`default`) is control-flow, not a predicate — evaluating it in the predicate pass makes it fire additively | resolved | [flows/switch-else-branch-fires-unconditionally.md](flows/switch-else-branch-fires-unconditionally.md) |
| 2026-07-01 | flows | a saved flow read cell (`jsonview`/`stat` over `flows.node_state`) showed **"binding broken — re-pick"** in the grid even though the backend read worked + the picker preview showed the value; repeatedly reported "fixed" because the passing tests hand-built a cell with **no** `cell.source`. Root cause: the gateway round-trips a v3 cell with an **empty v2 placeholder** `source:{tool:"",args:null}` beside the real `sources[]`; `WidgetView`'s `cell.source ?? <v3 target>` picked the truthy-but-empty placeholder → `flowBindingOfSource("")` → null → `denied` → "binding broken." Fixed to `cell.source?.tool ? cell.source : <v3 target>`; regression renders the EXACT round-tripped shape (placeholder included) through `WidgetHost`. Lesson: test the shape the STORE round-trips through the real render host; `??` is wrong when a field is present-but-empty | resolved | [flows/flow-read-binding-broken-empty-source.md](flows/flow-read-binding-broken-empty-source.md) |
| 2026-07-01 | flows | the sink `channel`/`inbox` target **sent no message on the live canvas** ("I add the channel name" but nothing lands). Not a code bug: the running dev node (binary built 05:04, started 05:04:30) **predated** the envelope-slice edits to the flows engine (`run_store.rs`/`node_state.rs`/`triggers.rs`/`mod.rs`, all 05:05–05:07), so the canvas drove the *old* sink/binding code while descriptors+tests were the new `payload` shape → the sink got no `payload`. Fixed by `make kill && make dev` (rebuild + restart) so the canvas runs the current tree; confirmed by the cron reactor firing post-restart. Durable guard: the dev node does NOT hot-reload Rust — if flows `src/*.rs` is newer than `target/debug/node`, restart before declaring the canvas broken | resolved | [flows/sink-channel-no-message-stale-node.md](flows/sink-channel-no-message-stale-node.md) |
| 2026-07-01 | flows | editing a flow node was **locked read-only until you refreshed the page** — after Stop, the config panel stayed disabled ("executed in the active run — read-only"), and an armed cron flow showed its last finite run's nodes as locked between firings. Root cause: the editor lock derived from the snapshot's *presence* (`snapshot ? executedNodeIds : ∅`), not from a genuinely in-flight run — and `cancelFlow` never updated the local snapshot (it relied on an SSE `run-finished` frame that never arrives once the stream has closed). Fixed (UI only) with a pure `lockedNodeIds` that locks only a **non-terminal** snapshot, a shared `isTerminalStatus`, and `markTerminal("cancelled")` on the cancel path so the lock releases instantly without a reload | resolved | [flows/node-config-locked-until-page-refresh.md](flows/node-config-locked-until-page-refresh.md) |
| 2026-06-30 | flows | the `sink` node could **never write** — its request shapes didn't match the target verbs: the `series` target sent `{series,value,ts}` to `ingest.write` (whose `Sample` needs `producer`/`seq`/`payload`) → **"missing field `producer`"**, and the `inbox` target sent `{channel,body}` to `inbox.record` (which needs a stable `id`) → **"missing arg: id"**. No sink-execution test existed (only descriptor-port assertions), so it shipped broken. Fixed by building valid bodies (series: full sample with stamped-by-verb `producer:""`, `seq:now`, `payload`; inbox: `id:{run}:{node}` + stringified body) + a real fail-before/pass-after regression that drives a run through each sink and reads the target back | resolved | [flows/sink-node-request-shapes-dont-match-target-verbs.md](flows/sink-node-request-shapes-dont-match-target-verbs.md) |
| 2026-06-30 | flows | the canvas "is this flow running?" banner read the **dormant** `flow.cron`/`next_attempt_ts` (no writer since triggers moved to per-node cursors), so an armed cron flow showed as **idle after a restart/reload**; and a headless (cron/source) flow had **no Stop button** at all (the existing Stop only renders for a non-terminal live run, which a finite-firing headless flow never has between firings). Fixed (UI only) by deriving the armed/enabled truth from the authoritative durable `flows.node_state` (+ graph-derived "scheduled" so it holds when disabled), and adding a durable **Deploy/Stop** toggle bound to `flows.enable` — the headless lifecycle switch, correct after restart | resolved | [flows/armed-banner-reads-dormant-cron-no-stop-for-headless.md](flows/armed-banner-reads-dormant-cron-no-stop-for-headless.md) |
| 2026-06-30 | flows | a flow could hold only **ONE trigger** — `flows.save` rejected a second cron ("a flow has one schedule"), and the same wall blocked two MQTT subs / webhooks; separately, one run fired the **whole graph** (every trigger at once), not "this trigger down its own wires". Root cause (design): reactive state (`flow.cron`/`flow.next_attempt_ts`) was hoisted to the **flow**, and `create_run` enqueued every indegree-0 node. Fixed by moving the cursor to a per-node `flow_trigger_state` (the reactor scans **trigger nodes**, N independent), per-wire **subgraph** runs (`reachable_from`/`indegrees_within` + `create_run{entry}`), and dropping the single-schedule rejection. Also shipped a real stateful `counter` (durable `flow_node_memory` + atomic `lb_store::increment`) for the "count goes up" ask | resolved | [flows/flow-level-cron-rejects-multiple-triggers.md](flows/flow-level-cron-rejects-multiple-triggers.md) |
| 2026-06-30 | flows | opening an armed flow showed a **frozen last-run "DONE" snapshot** + a contradictory "54 runs / no runs yet" banner; "the count isn't going up". Root cause (design): the canvas's only runtime view was a finite `flows.runs.get` snapshot, but the spec's **persistent per-node state** (Decision 5 `flow_node_state`, last-value updated in place each scan — already WRITTEN by `record_outcome`) had **no read verb** and was never painted. Fixed by adding `flows.node_state` (+ `GET /flows/{id}/node_state`), painting it as the canvas BASE steady-state with the run snapshot overlaid, an honest armed banner, and deterministic multi-trigger cron derivation. Live: node-state rev advanced 59→60 on a cron firing (updated in place) | resolved | [flows/canvas-shows-finite-run-not-persistent-node-state.md](flows/canvas-shows-finite-run-not-persistent-node-state.md) |
| 2026-06-30 | flows | a `mode:cron` flow trigger **never fired** ("added a cron trigger every min, count never goes up") — two disconnected gaps: (1) `react_to_flows_cron`/`reconcile_flows` were only ever called from tests — **no production loop ticked them**; (2) the canvas writes the schedule to the trigger node's `config.cron` but the reactor scans the top-level `flow.cron`, and `flows.save` never derived one from the other. Fixed by `spawn_flow_reactors` (a detached per-node tick over the configured ws, live clock) wired into node boot + `derive_cron_from_trigger` at save + switching the node binary to `Gateway::new_live`. Live: a `* * * * *` trigger now fires every minute on its own, runs settle with real values | resolved | [flows/cron-trigger-never-fires-no-reactor-driver.md](flows/cron-trigger-never-fires-no-reactor-driver.md) |
| 2026-06-30 | flows | the run id was **constant for the node's whole uptime** — `gw.now` was frozen at gateway construction and the manual run id was `"{flow_id}-run-{now}"`, so every `flows.run` for a flow resolved to one id. That re-drove the same terminal run (churn + flickering Stop/Resume + "no values") AND made overlapping runs hammer the same records (the rev race below). Live: 8 concurrent runs returned the SAME id + a wall of conflicts. Fixed by minting a ULID per manual run (caller-supplied id still honored) + unfreezing `gw.now` to a live accessor (fixed-clock test seam kept) | resolved | [flows/frozen-gw-now-collides-run-ids.md](flows/frozen-gw-now-collides-run-ids.md) |
| 2026-06-30 | flows | run-store / `lb-jobs` writes raced the store's monotonic `rev` RMW under concurrent same-record writers → `Invalid revision '174'` / `read or write conflict … can be retried` (made certain by the constant run id above). `lb_store::write` is atomic per write but not isolated across two writers of one `table:id`. Fixed with a store-level `write_locked` (per-`(ws,table,id)` async lock + bounded retry-on-conflict, the `capped_insert` shape) adopted by run_store + jobs, plus a create-if-absent idempotent seed | resolved | [flows/run-store-rev-conflict-under-concurrency.md](flows/run-store-rev-conflict-under-concurrency.md) |
| 2026-06-30 | flows | backgrounding the manual run (`tokio::spawn(coordinator::drive)`) failed to compile — "future cannot be sent between threads safely", the compiler pointing at `flows_run_async` itself. Not a real non-`Send` capture (a standalone `assert_send(drive(...))` passed) but an **async opaque-type recursion**: a `tool` node can call `flows.run`, so `flows_run_async → drive → tool → dispatch → flows_run_async` is an infinitely-recursive `impl Future` the compiler can't size or prove `Send`; boxing the `.await`s didn't help (the cycle is in the type, not the indirection). Fixed by type-erasing the recursion edge to a **concrete** `call_flows_tool_boxed` (`Pin<Box<dyn Future + Send>>`) at the dispatch boundary | resolved | [flows/async-run-not-send-recursion.md](flows/async-run-not-send-recursion.md) |
| 2026-07-02 | datasources | Datasource **Test**/Save/Remove failed with an opaque `denied` (403) while `datasource.list` was allowed and the sidecar was installed+healthy — the DSN secret was written owned by whoever ran `datasource.add` (the boot seed's `ext:federation-bootstrap`, or `user:ada`) but read/deleted under the fixed `ext:federation` mediator, so the secrets **owner wall** (gate 3) denied every cross-admin overwrite/remove; all of `Denied`/`EndpointRefused`/owner-denial collapsed to the same `"denied"`. Fixed by owning every DSN under the stable `ext:federation` principal (`store_dsn`/`forget_dsn`), adding `lb_secrets::reclaim` (write-and-take-ownership → poisoned stores self-heal on next add), and a distinct `SecretUnavailable` error. Full CRUD lifecycle green vs. real Postgres; regression tests in `secrets` + `host` | resolved | [datasources/test-denied-secret-owner-wall-across-admins.md](datasources/test-denied-secret-owner-wall-across-admins.md) |
| 2026-06-30 | observability | `capped_insert` overgrew the cap intermittently under concurrency (`count == cap + k`) — SurrealDB `kv-mem` snapshot isolation let two concurrent same-key trims each compute `$keep` without seeing the other's just-inserted row, so each under-deleted. The insert+trim was already one transaction (atomic ≠ isolated); no write-conflict is raised to retry on. Fixed by serializing the insert+trim per `(ns, table, cap_key)` bucket with an in-process async lock — different keys never contend; the concurrency test (100 inserts at 5× cap) now passes 10/10 | resolved | [observability/capped-insert-overgrows-cap-under-concurrency.md](observability/capped-insert-overgrows-cap-under-concurrency.md) |
| 2026-06-30 | host | `flows.get` on another workspace's (or a deleted) flow returns `403 not permitted`, not `404` — `FlowsError::NotFound` collapses to `ToolError::Denied` (the MCP existence-hiding discipline: a non-owner never learns whether a flow exists vs. is forbidden). Intentional; the Wave-3 canvas/gateway tests assert the opaque `403` (the picker only lists reachable flows). Nothing to fix — callers expecting "not found" must expect `Denied` | documented (intentional) | [host/flows-get-absent-collapse-to-denied.md](host/flows-get-absent-collapse-to-denied.md) |
| 2026-06-30 | build | host build dies `zigranlib: not found` building vendored OpenSSL; new Docker build dies `linker 'zigcc' not found` — `rust/.cargo/config.toml` pins a host-only zig toolchain that leaks into the mounted container. Added `docker/build/` (real Debian GCC cross-toolchains, no zig) and made `build.sh` override the host CC/AR/RANLIB + `[target.*].linker` | resolved | [build/host-cargo-config-zig-leaks-into-container.md](build/host-cargo-config-zig-leaks-into-container.md) |
| 2026-06-29 | datasources | the Datasources detail page couldn't list tables — `plan: ... table 'datafusion.information_schema.tables' not found` (DB was up + seeded). The federation engine only registers the tables a query *references* as DataFusion providers, so the UI's hand-written `information_schema`/`pg_class` discovery SQL (shipped through `federation.query`, duplicated in `useDatasourceQuery` + the palette's `useSqlSchema`) was unplannable. Fixed by routing discovery through the native `federation.schema` verb (which already existed + is covered by a real gateway test); deleted both copies of the catalog SQL and the dead `kind` plumbing | resolved | [datasources/discovery-via-information-schema-sql-unplannable.md](datasources/discovery-via-information-schema-sql-unplannable.md) |
| 2026-06-29 | channels | every `query_result` came back `chart:null` even for a clean temporal/numeric shape — `federation.query` returns column-aligned **array** rows but `chart::pick_chart` keys cells by column **name** (`row.get(col)`), so the picker matched nothing and always fell through to `None`. The deny-path test never reached real rows, so it hid until the sqlite happy-path test ran. Fixed with `query_worker::keyed_rows` (zip arrays→objects for the picker only; the persisted payload keeps array rows); a unit regression + the end-to-end gateway test assert a real non-null line chart | resolved | [channels/query-result-chart-always-null.md](channels/query-result-chart-always-null.md) |
| 2026-06-29 | channels | every untruncated `query_result` item silently parsed back as plain **chat** — `QueryResultPayload::truncated` had `skip_serializing_if` (drops the `false` case from the wire) but no matching `#[serde(default)]`, so deserialization failed with `missing field 'truncated'`; `parse_payload` swallows that to `None`, so the result card/chart never rendered. Fixed by adding `#[serde(default)]`; a round-trip regression test fails-before/passes-after | resolved | [channels/query-result-missing-truncated-field.md](channels/query-result-missing-truncated-field.md) |
| 2026-06-29 | reminders | the UI's `reminder.create` crashed every time with `bad input: CronScheduler time search limit exceeded` — `useReminders` passed `Date.now()` (**ms**) as the host's logical `ts`, which is **seconds**; the host fed the ~1.7e12 value to `next_after`/croner as a year-~55000 anchor and croner aborted its forward search. Fixed by converting at the seam (`nowSecs() = Math.floor(Date.now()/1000)`); the gateway test asserting `nextAttemptTs > 0` fails-before/passes-after | resolved | [reminders/ts-unit-mismatch-cron-search-limit.md](reminders/ts-unit-mismatch-cron-search-limit.md) |
| 2026-06-29 | frontend | Phase-3 `viz.query` swap: dashboard gateway panels rendered EMPTY (`Unable to find label "timeseries line"`) — `mcp:viz.query:call` was absent from the dev session's `member_caps()`, so the new render verb was denied; plus a couple of gateway tests seeded without `ingest.write`/`tags.add`. Fixed by adding `mcp:viz.query:call` to `member_caps` + granting the seed caps; the swap's deny path was correct (host write path proven sound headlessly) | resolved | [frontend/gateway-seed-series-500-denied-preexisting.md](frontend/gateway-seed-series-500-denied-preexisting.md) |
| 2026-06-29 | host | `rules.list` / `chains.list` returned an empty roster even with saved records — both list verbs decoded the `lb_store::scan` row directly, but `scan` returns the `{data, rev}` envelope (the record is under `row.data["data"]`), so every `from_value` silently failed; `chains_list` also read its `deleted` tombstone off the envelope. Fixed by unwrapping the envelope before decoding (mirroring `scan_dashboards`), with a roster-contains regression test | resolved | [host/rules-chains-list-drops-every-row-envelope.md](host/rules-chains-list-drops-every-row-envelope.md) |
| 2026-06-28 | host-tools | `offline_sync_test` flaked `applied: 0` under parallel `cargo test --workspace` — `replay_history` published before the hub's `sync_channel` subscription propagated (and the pair raced multicast discovery); fixed with a publisher-side `await_subscriber` readiness barrier + a deterministic loopback-TCP link | resolved | [host-tools/offline-sync-replay-races-subscription.md](host-tools/offline-sync-replay-races-subscription.md) |
| 2026-06-28 | bus | a routed cross-node tool call (`cross_node_routing_test`) timed out under parallel `cargo test --workspace` — the two in-process peers raced (and under load never completed) ambient multicast discovery; fixed by linking the pair over an explicit loopback TCP endpoint | resolved | [bus/routed-call-races-mesh-discovery.md](bus/routed-call-races-mesh-discovery.md) |
| 2026-06-28 | host-tools | default parallel `cargo test --workspace` timed out in existing `cross_node_routing_test`; the same test passed isolated/serially and full workspace passed with `--test-threads=1` | resolved (superseded by [bus/routed-call-races-mesh-discovery.md](bus/routed-call-races-mesh-discovery.md)) | [host-tools/cross-node-routing-parallel-timeout.md](host-tools/cross-node-routing-parallel-timeout.md) |
| 2026-06-28 | agent | `run_session` resume re-derived the conversation from the goal (opaque-`String` step) → a resumed run re-asked the model from scratch instead of continuing (unsafe once a run can suspend mid-conversation) | resolved | [agent/resume-re-derived-from-goal-not-transcript.md](agent/resume-re-derived-from-goal-not-transcript.md) |
| 2026-06-28 | store | store-managed `rev` bump silently stayed at 1 — a `SELECT VALUE rev FROM ONLY … [0]` indexes a scalar (not a set) → `NONE` → coalesced to the first-rev default on every write | resolved | [store/rev-subquery-always-returns-first.md](store/rev-subquery-always-returns-first.md) |
| 2026-06-28 | extensions | devkit build logs published as raw bytes arrive as `null` over `/bus/stream`, whose payload contract is JSON | resolved | [extensions/devkit-build-log-bus-stream-null.md](extensions/devkit-build-log-bus-stream-null.md) |
| 2026-06-28 | frontend | an installed extension's `[[widget]]` tile renders blank — the iframe tier can't resolve its bare `react` imports (`Failed to resolve module specifier "react"`); the remote externalizes React to the shell import map, which only exists in-process | resolved | [frontend/ext-widget-iframe-tier-cannot-resolve-bare-react.md](frontend/ext-widget-iframe-tier-cannot-resolve-bare-react.md) |
| 2026-06-27 | frontend | a render-time `<Navigate to={dynamicHref}>` (the tenant-less `/t/<ws>` redirect fallback) re-navigates every commit → "Maximum update depth exceeded" (~370 warnings; tests still pass) | resolved | [frontend/navigate-dynamic-href-render-loop.md](frontend/navigate-dynamic-href-render-loop.md) |
| 2026-06-27 | extensions | a WIT `@0.2.0` minor bump breaks every `@0.1.0` guest at instantiation (wasmtime treats a `0.x` minor as semver-incompatible at link time, though the loader's major-check passes) | resolved | [extensions/wit-minor-bump-breaks-0_1-guest-linking.md](extensions/wit-minor-bump-breaks-0_1-guest-linking.md) |
| 2026-06-27 | store | SurrealKV "Invalid revision N for type Value" on the SECOND ingest drain (persistent store only; `mem://` is fine) — durable on-disk ingest unreliable past one write/ws | documented (engine-level; demo runs on `mem://`) | [store/surrealkv-invalid-revision-on-drain-reread.md](store/surrealkv-invalid-revision-on-drain-reread.md) |
| 2026-06-27 | build | Rust build fails `linker 'cc' not found` — no C compiler on the box and no root to apt-install one (`ring` also needs to compile C) | resolved | [build/no-c-compiler-linker-cc-not-found.md](build/no-c-compiler-linker-cc-not-found.md) |
| 2026-06-27 | extensions | the host-mediated bridge (`/mcp/call`) can't dispatch a host-native `series.*` verb — a federated page's reads `NotFound`/403 | resolved | [extensions/bridge-cannot-dispatch-host-native-series.md](extensions/bridge-cannot-dispatch-host-native-series.md) |
| 2026-06-27 | extensions | `series.find` can't discover a series seeded with `labels` — the ingest write path never tags it | documented | [extensions/series-find-needs-tag-edges-not-labels.md](extensions/series-find-needs-tag-edges-not-labels.md) |
| 2026-06-27 | build | the whole desktop hard-freezes (forced reboot) during Rust builds; no OOM log (suspected swap death-spiral from parallel `rust-lld`) | mitigated | [build/host-freezes-during-rust-build.md](build/host-freezes-during-rust-build.md) |
| 2026-06-27 | extensions | a federated extension page won't load in the Vite **dev server** (`make dev`/`make ui`) — `getUrl(...).then is not a function`; the federation host runtime only exists in a production build | worked-around (`make ui-preview`) | [extensions/federated-remote-fails-in-dev-server.md](extensions/federated-remote-fails-in-dev-server.md) |
| 2026-06-27 | tags | `DEFINE TABLE … AS SELECT … GROUP` defines but never populates on SurrealKV (tag_counts empty) → per-query | resolved | [tags/materialized-view-does-not-populate.md](tags/materialized-view-does-not-populate.md) |
| 2026-06-27 | tags | HNSW `<\|K\|>` knn returns nothing; the two-arg `<\|K,EF\|>` form is required | resolved | [tags/hnsw-knn-needs-ef-arg.md](tags/hnsw-knn-needs-ef-arg.md) |
| 2026-06-27 | tags | `type::thing("series:node.cpu_temp")` mis-parses a dotted entity id (tag add fails) | resolved | [tags/dotted-entity-id-needs-two-arg.md](tags/dotted-entity-id-needs-two-arg.md) |
| 2026-06-27 | tags | a `tagged` edge silently drops fields literally named `key`/`value` | resolved | [tags/relation-drops-key-value-fields.md](tags/relation-drops-key-value-fields.md) |
| 2026-06-27 | ingest | `DELETE … ORDER BY … LIMIT 1` unsupported (drop-oldest eviction) | resolved | [ingest/delete-order-by-limit-unsupported.md](ingest/delete-order-by-limit-unsupported.md) |
| 2026-06-27 | ingest | `series.read(seq <= u64::MAX)` returns nothing (huge int coerces to float) | resolved | [ingest/u64-max-bound-coerces-to-float.md](ingest/u64-max-bound-coerces-to-float.md) |
| 2026-06-27 | store | workspace fails to build: modules referenced but never declared (`mod …` missing) | resolved | [store/half-wired-modules-block-workspace-build.md](store/half-wired-modules-block-workspace-build.md) |
| 2026-06-26 | extensions | a ws-B caller can run an extension only ws-A installed (the loaded instance is node-global; the wall is caps + store, not the instance) | resolved | [extensions/loaded-extension-instance-is-node-global.md](extensions/loaded-extension-instance-is-node-global.md) |
| 2026-06-26 | agent | the agent is `Denied` reading a substrate doc the caller owns (derived-sub vs the S4 membership gate) | resolved | [agent/agent-reads-doc-it-doesnt-own-is-denied.md](agent/agent-reads-doc-it-doesnt-own-is-denied.md) |
| 2026-06-26 | store | `DEFINE BUCKET` fails to parse on the embedded `kv-mem` store (assets stored as records instead) | resolved | [store/define-bucket-unavailable-in-kv-mem-build.md](store/define-bucket-unavailable-in-kv-mem-build.md) |
| 2026-06-26 | bus | `cargo test --workspace` OOM-killed (137) once tests boot 2 nodes each | resolved | [bus/cargo-test-workspace-ooms-with-many-peers.md](bus/cargo-test-workspace-ooms-with-many-peers.md) |
| 2026-06-26 | bus | a live subscriber receives a message published by a *different* test | resolved | [bus/in-process-peers-share-the-keyspace.md](bus/in-process-peers-share-the-keyspace.md) |
| 2026-06-26 | store | `ORDER BY data.ts` fails: "Missing order idiom in statement selection" | resolved | [store/order-by-needs-selected-idiom.md](store/order-by-needs-selected-idiom.md) |
| 2026-06-26 | store | `.content()` rejects raw `serde_json::Value` | resolved | [store/content-rejects-serde-json-value.md](store/content-rejects-serde-json-value.md) |
| 2026-06-26 | bus | booting a Node in a test panics: Zenoh needs a multi-thread runtime | resolved | [bus/zenoh-needs-multi-thread-runtime.md](bus/zenoh-needs-multi-thread-runtime.md) |
| 2026-06-26 | auth | a freshly-minted token fails verification with BadToken | resolved | [auth/valid-token-fails-verification.md](auth/valid-token-fails-verification.md) |
