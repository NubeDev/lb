# Flows — public docs

TODO: the full flows write-up lands as the flows scopes ship (see `docs/scope/flows/README.md`).

Shipped so far from this topic's UI track — **flow-ui-polish** (2026-07-08): the editor chrome is
consolidated (Deploy · Run⇄Stop · Pause⇄Resume · Debug · `⋯` overflow), Config | Debug share one
resizable right dock, the node config form is schema-described with one context-aware primary
action, and export/import is a preview dialog (pretty/compact, copy, selected-nodes scope,
paste-import). Details: `docs/sessions/flows/flow-ui-polish-session.md`.
