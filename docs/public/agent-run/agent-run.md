# Agent-run (public)

Status: TODO — filled when the agent-run slice ships. See the ask in
`../../scope/agent-run/agent-run-scope.md`.

The run as a first-class object: a canonical `RunEvent` stream, an ACP stdio adapter, per-tool-call
Allow/Deny/Ask with durable suspend/resume, and model-activated skills. Promote the shipped truth
here (event vocabulary, the `agent.watch` surface, the ACP role, the permission policy record, the
skill catalog) once built and tested.
