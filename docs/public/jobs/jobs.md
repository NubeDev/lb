# Jobs

TODO: Document the shipped job queue, worker behavior, retry policy, and scheduling model.

TODO (on ship of `scope/jobs/job-retention-scope.md`): the reactor drain scan (indexed
`pending`, O(pending) not O(table)) and terminal-job retention (bounded `job` / `flow_run` /
`flow_step_output` growth).

