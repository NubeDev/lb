# Identity, auth & capabilities

TODO — public documentation for the auth-caps surface, filled when shipped truth is promoted from
`docs/scope/auth-caps/`.

Covers: the capability grammar, tokens, workspaces/teams/members/grants, and login. The
**login-hardening** work (a real credential check replacing the password-less dev-login, and
role-scoped capability issuance so a member is not silently an admin —
`docs/scope/auth-caps/login-hardening-scope.md`) promotes here once shipped.
