# App — the React Native mobile app (shipped truth)

> **TODO** — nothing has shipped yet. The ask lives in `docs/scope/app/`
> (read `docs/scope/app/README.md` for the read order: shell → extensions → sdk).
>
> Fill this in as slices ship: transport + session, extension mount contract,
> `@nube/app-sdk` exports, and the two reference extensions
> (`proof-panel-app`, `channel-chat`).
