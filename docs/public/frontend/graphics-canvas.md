# Graphics canvas

TODO — not yet shipped. The ask lives at `../../scope/frontend/graphics-canvas-scope.md`;
the phases 1–2 implementation scope co-locates with the extension at
`rust/extensions/thecrew/docs/thecrew-extension-scope.md` (control-engine precedent —
the extension keeps the playground's id `thecrew`):
a free-form, data-bound graphics surface (plant graphics / floor plans / 3D buildings)
shipped as a **100% UI extension** — one engine (three.js via `@react-three/fiber`,
flat + 3D from the same scene document), drawable by hand and by an AI agent through
the shipped asset/document verbs, with new equipment arriving as symbol packs
(GLTF/SVG assets — data, not code).

Filled in when phase 1 (the viewer) ships.
