# Graphics canvas

TODO — not yet shipped. The ask lives at `../../scope/frontend/graphics-canvas-scope.md`:
a free-form, data-bound graphics surface (plant graphics / floor plans / mimic pages) —
a `scene:{id}` document rendered as a `view:"canvas"` widget, drawable by hand, by an AI
agent via `scene.*` verbs, and extensible with `[[shape]]` extension shape types.

Filled in when the first phase ships.
