# Dashboard (public)

TODO — filled when the dashboard surface ships. The ask is
`scope/frontend/dashboard-scope.md`: a first-party grid-of-widgets dashboard over real series
(Phase 1, seeded), widgets-as-extensions (Phase 2, federation), and the real edge fleet (Phase 3).

Promote durable truth here once Phase 1 lands (the `dashboard.*` CRUD verbs, the series live SSE
route, the built-in chart/stat/gauge widgets) — see `HOW-TO-CODE.md` step 7.
