# Agent memory

TODO — not yet shipped. The ask: `../../scope/agent-memory/agent-memory-scope.md` (durable,
access-walled agent memory in the MEMORY.md shape: fact records + a derived index, read/written
over caps-checked `agent.memory.*` verbs under the derived principal).

This file is filled in when the slice ships (per `../../ABOUT-DOCS.md`).
