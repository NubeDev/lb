# Desktop (Tauri shell packaging)

TODO — filled when the packaging slice ships.

The ask: build the existing Tauri v2 shell (`ui/src-tauri`, `lazybones-shell`) as plain
executables (no AppImage/installer) for Linux x86-64 and Windows x86-64, with a CI matrix and a
boot smoke test. See `docs/scope/desktop/desktop-packaging-scope.md`.
