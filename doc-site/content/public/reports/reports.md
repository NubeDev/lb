# Reports

TODO — filled when the report builder ships.

The ask: `docs/scope/reports/report-builder-scope.md` — a notebook-style report builder
(markdown + images + existing dashboard panels as ordered blocks), reusable per-workspace
brand profiles, and branded PDF export via a pure Typst render crate (`lb-render`).
