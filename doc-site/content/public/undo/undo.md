# Undo — the reversible-command journal

> **TODO — exposure in flight.** The mechanism is shipped and green (store-managed `rev`,
> before-image journal, conditional stale-refusing restore, cap-gated `undo`/`redo`/`history.*`
> verbs, auto-capture-on-dispatch classified from runtime outbox taint), but it is not yet
> reachable from the product: role grants, typed gateway routes, and the shell affordance are the
> current slice. This page is filled when that ships.
>
> - The ask: `docs/scope/undo/undo-scope.md` (mechanism) ·
>   `docs/scope/undo/undo-exposure-scope.md` (exposure — building)
> - The working logs: `docs/sessions/undo/`
