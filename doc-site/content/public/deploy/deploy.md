# Deploy

TODO (stub). Promoted from `docs/scope/deploy/` when the Fly.io deploy ships.

Will document: deploying a Lazybones `node` to Fly.io as a single-container, Caddy-fronted
standalone node with an embedded SurrealDB store on a persistent volume and the federation
sidecar wired for SQLite datasources — plus the shared build assets in `deploy/common/`
reused across local Docker, GitHub CI, and Fly.

Scope: [`docs/scope/deploy/fly-deploy-scope.md`](../../../../docs/scope/deploy/fly-deploy-scope.md).
