# Caching

> TODO — filled when the optional gateway response cache ships.
> The ask: `docs/scope/caching/response-cache-scope.md` (moka hot tier + SurrealDB warm
> tier behind the caps wall, `page-cache` cargo feature + `BootConfig.cache`, no Redis).
