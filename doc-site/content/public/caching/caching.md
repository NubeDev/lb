# Caching

> TODO — filled when the optional gateway response cache ships.
> The ask: `docs/scope/caching/response-cache-scope.md` (moka-only v1 behind the caps wall:
> single-flight, generation invalidation, time-bucket quantisation, `cache.stats`/`cache.purge`;
> `page-cache` cargo feature + `BootConfig.cache`; no Redis; warm tier is a conditional v2).
