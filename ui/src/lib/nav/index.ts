// The nav library barrel (nav scope) — the user-/team-authored navigation menu asset. Re-exports the
// wire client + types so features import from `@/lib/nav`.

export * from "./nav.api";
export * from "./nav.types";
