// The store-schema reader — shared by the dashboard SQL builder and the rules data explorer.
export { readSchema } from "./schema.api";
export type { Schema, SchemaTable, SchemaColumn } from "./schema.api";
