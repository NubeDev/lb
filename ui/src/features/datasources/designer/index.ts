// The schema-designer feature barrel (schema-designer scope). The routing layer imports the list +
// page from here. One responsibility per file; the barrel is the single import seam (FILE-LAYOUT).

export { SchemaDesignerList } from "./SchemaDesignerList";
export { SchemaDesignerPage } from "./SchemaDesignerPage";
