// The theme feature's public surface: the customizer control panels, now hosted in Settings
// (Settings → Theme). The old nav-footer `ThemeSwitcher`/`Customizer` sheet were removed in favor of
// the Settings tab. `ThemeTab` = presets/radius/mode/import/brand-colors + admin workspace default;
// `LayoutTab` = sidebar variant/collapsible/side.

export { ThemeTab } from "./ThemeTab";
export { LayoutTab } from "./LayoutTab";
