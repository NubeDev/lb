// Barrel for the unified extensions console (re-exports only — FILE-LAYOUT). Supersedes the retired
// features/registry + features/native demo views.

export { ExtensionsView } from "./ExtensionsView";
export { UploadArtifact } from "./UploadArtifact";
export { useExtensions } from "./useExtensions";
