// Selection state + raycast plumbing: click select, shift-add, screen-space box
// select over projected bounds (validate perf at ~200 shapes — builder-ux open q),
// Esc clears. Writes selection into scene-store.

export function useSelection(): void {
  // TODO(phase 2)
}
