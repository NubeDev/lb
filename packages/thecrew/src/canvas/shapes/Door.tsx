// plan.door — snaps into a wall segment, cuts it. Flat: arc-swing glyph · 3D: wall
// gap + leaf. Props: width, swing side.

import type { ShapeComponentProps } from "./shape-props";

export function Door(_props: ShapeComponentProps) {
  return null; // TODO(phase 3)
}
